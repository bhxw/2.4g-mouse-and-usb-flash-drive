# nrfrx-usb 项目架构说明

> 更新：2026-09-06 ｜ 文档状态：随代码演进持续维护（改代码/加模块必须同步更新本文档 + [开发日志.md](开发日志.md)）
> 关联文档：[接收端开发方案.md](接收端开发方案.md)（执行计划/验收）｜ [开发日志.md](开发日志.md)（变更与排障流水）｜ mouse.ioc（CubeMX 工程）

---

## 1. 项目概述

本仓库是 **2.4G 空中鼠标系统的接收端（RX）**：一块 USB 接收器硬件，通过 nRF24L01 接收发射端（TX）发来的鼠标数据包，再以 **USB HID 鼠标**上报给主机。发射端是另一个工程，其主函数参考副本 `txmain.c` 放在本仓库根目录供对照。

**当前定位**：**USB MSC+HID 复合设备已验证可用**（同一 USB 口既是鼠标接收器又是 U 盘），SD 卡数据日志链路已实现。
**后续目标**：性能优化（SD DMA/多块传输）+ 可靠性收尾（M4 看门狗/24h 测试），最终替换为自研微内核（M5）。升级范围以 [接收端开发方案.md](接收端开发方案.md) 为准。

系统全貌：

```
┌─────────────┐   2.4G (nRF24L01, 2Mbps)   ┌──────────────────────────────┐
│  TX 发射端   │ ─────────────────────────▶ │ RX 接收端（本仓库）            │
│ STM32F103   │  32B 载荷 x/y/按键/gx/gy/gz │ nRF24L01 ─▶ USB HID 上报主机    │
│ MPU6050 姿态│                            │ (SPI2)     └─▶ (目标) SD 卡日志 │
└─────────────┘                            └──────────────────────────────┘
```

## 2. 硬件平台

| 项 | 值 | 说明 |
|---|---|---|
| MCU | STM32F103C8T6 | LQFP48；64KB Flash / 20KB RAM |
| 时钟 | HSE 8MHz，PLL ×9 → 72MHz | USB 时钟 48MHz（PLL/1.5） |
| 调试 | SWD（PA13/PA14） | ST-Link |
| USB | FS 设备（PA11 DM / PA12 DP） | HID+MSC 复合设备（鼠标+U盘） |
| 2.4G | nRF24L01+ 模块 | SPI2 + CE/CSN/IRQ |
| 姿态 | （RX 板无；TX 板为 MPU6050） | RX main 中有遗留初始化调用，待清理 |
| SD | 已实现（`App/Src/sd_spi.c`）；模块 **VCC=5V**（板载 LDO→3.3V + 电平转换 3.3/5V 兼容） | SPI1 默认映射 PA5/6/7 + CS=PB12；命令阶段用 HAL，数据阶段用直接寄存器全双工传输（避免 HAL 方向切换）；DMA 未启用（预留）；用前格式 FAT16/FAT32 |
| OLED | PB10/11 软件 I2C（可转移） | 调试显示 |

## 3. 目录结构

```
nrfrx-usb/
├── Core/                    CubeMX 生成 + 系统入口
│   ├── Inc/  Src/           main.c / stm32f1xx_it.c / gpio / i2c / spi / tim / usart / system
├── App/                    ★ 应用层（原 MDK-ARM/user，2026-09-05 迁出）
│   ├── Src/                 NRF24L01.c / NRF_Demo.c / OLED.c / sd_spi.c / diskio.c / usb_storage.c / usbd_composite.c
│   └── Inc/                 对应头文件（NRF24L01/NRF_Demo/OLED/sd_spi/diskio/usb_storage/usbd_composite）
├── Reference/              TX 端参考代码（txmain.c，仅对照不编译）
├── MDK-ARM/
│   ├── freertos/            FreeRTOS 内核源码（tasks/queue/semphr/heap_4…）
│   ├── mouse.uvprojx        Keil 工程（用户代码组 App 指向 App/Src+Inc）
│   └── …                    编译产物不入库
├── Middlewares/ST/STM32_USB_Device_Library/   ST USB 设备库（Core + HID/MSC 类）
├── USB_DEVICE/App  Target/   CubeMX USB 工程文件（usb_device/usbd_desc/usbd_conf）
├── Drivers/  CMSIS/HAL       标准外设库
├── txmain.c                  TX 端主函数参考副本（仅对照，不参与编译）
├── mouse.ioc                 CubeMX 工程
└── 文档：简历.md / 接收端开发方案.md / 项目架构.md / 开发日志.md
```

## 4. 引脚与总线

### 4.1 现状（本仓库实际接线）

| 功能 | 引脚 | 备注 |
|---|---|---|
| 晶振 | PD0/PD1 | HSE |
| SWD | PA13/PA14 | |
| USB | PA11/PA12 | DM/DP |
| nRF | SPI2 = PB13(SCK)/PB14(MISO)/PB15(MOSI)；CE=PB0；CSN=PB1；IRQ=PB5 | |
| OLED | PB10/PB11 | 软件 I2C |
| UART1 | PA9/PA10 | printf 调试（现状闲置，拟保留） |
| GPIO 输入 | PA3/PA4（下拉） | 遗留按键输入，当前无实际用途 |
| LED | PA5 | 调试指示灯 |

### 4.2 目标变更（升级后，详见方案 M0）

| 变更 | 引脚 | 原因 |
|---|---|---|
| SD 新增 | SPI1 默认映射 SCK=PA5/MISO=PA6/MOSI=PA7 + CS=PB12（DMA 预留，当前阻塞式批量传输） | 独立 SPI，避免与 nRF 争用 SPI2（PA5 原 LED 让出） |
| UART1 | 保留（默认） | printf 调试台 |

## 5. 数据流

**现状（RX 主循环）**

```
nRF24L01(RX)  SPI2 轮询 10ms ─▶ MousePacket_t{buttons,x,y,…}
      └─▶ USBD_HID_SendReport(4B: buttons,x,y,0) ─▶ 主机
OLED 定时刷新链路状态
```

**当前实现（M3 已验证）**

```
nRF IRQ → 信号量 → [rf_rx_task] 收包 → HID 上报(EP1 IN) → USB 主机
                          │
                          ├─▶ 环形缓冲(RAM 4~8KB) ─▶ [sd_log_task] 批量写 FatFs ─▶ SD(SPI1 阻塞)
USB: 主机 ⇄ USBD(Composite: HID+MSC)
      ├─ HID: EP1 IN 中断(4B) ← rf_rx_task 上报鼠标
      └─ MSC: EP2 OUT/IN 批量(64B) → USB ISR 信号 scsi_msc_task → SD 扇区访问（任务上下文）
      主机枚举占用 SD → 暂停 sd_log_task（单写者闸门）→ 拔出恢复
```

## 6. 现有代码模块要点（用户代码）

| 模块 | 职责 | 关键点 / 问题 |
|---|---|---|
| `Core/Src/main.c` | RX 主循环 | 初始化顺序：SD preinit → USB 启动；HAL tick 由 SysTick 单源驱动 |
| `stm32f1xx_it.c` | 中断 | SysTick 单源 HAL_IncTick；USB IRQ 优先级 (5,0) 兼容 FreeRTOS |
| `App/Src/NRF24L01.c` + `App/Inc/NRF24L01.h` | nRF 驱动 | 寄存器级；固定 32B 载荷；2Mbps/16bit CRC/自动应答重发(10)；RX 轮询 STATUS；地址/通道写死 |
| `App/Src/NRF_Demo.c` + `App/Inc/NRF_Demo.h` | 收发 Demo | 角色宏 DEMO_ROLE；固定演示包循环；OLED 状态显示；**实际 main 未调用 Run** |
| `App/Src/sd_spi.c` + `App/Inc/sd_spi.h` | SD 卡 SPI 驱动 | SPI1 阻塞式（直接寄存器全双工）；CMD0/CMD8/ACMD41/CMD58 初始化；CMD17/CMD24 单块读写；CMD9 CSD 容量解析；18MHz 数据传输；DMA 预留 |
| `App/Src/diskio.c` + `App/Inc/diskio.h` | FatFs 磁盘 I/O 适配 | 卷 0=SD（接 sd_spi）；单/多扇区读写；ioctl 仅 CTRL_SYNC/GET_BLOCK_SIZE |
| `App/Src/usb_storage.c` + `App/Inc/usb_storage.h` | USB MSC 存储后端 | SCSI 回调（IsReady/Read/Write/GetCapacity/Inquiry）→ sd_spi；LUN0=SD 卡；开机预缓存容量；创建 scsi_msc_task 信号队列 |
| `App/Src/usbd_composite.c` + `App/Inc/usbd_composite.h` | USB HID+MSC 复合类包装器 | 旧版 Core 单类限制下的 pClassData 切换路由；Setup 按 wIndex 分发，DataIn/Out 按 EP 号分发；配置描述符 57B |
| `usbd_msc_scsi.c`（ST 库修改） | SCSI 延迟处理 | ProcessRead/Write 设标志+信号队列 → scsi_msc_task 执行 SD I/O → 续传 BOT 状态机；Read10 已修复双更新 bug |
| USB HID+MSC | ST 库 + 复合模式 | HID: EP1 IN 中断 4B；MSC: EP2 OUT/IN 批量 64B；由复合类统一管理 |

## 7. 已知问题清单（评审遗留，处理归属里程碑）

| # | 问题 | 位置 | 处理 |
|---|---|---|---|
| 1 | ~~HAL tick ≈ 2.1× 真实速率~~ | stm32f1xx_it.c | ✅ M0 已修复（SysTick 单源） |
| 2 | ~~RX main 死代码/第二个 while 不可达~~ | Core/Src/main.c | ✅ M0 已清理 |
| 3 | ~~EXTI3/4 回调不可达~~ | stm32f1xx_it.c | ✅ M0 已核实清理 |
| 4 | NRF 发送阻塞最长 100ms | NRF24L01.c | M4 事件化（RX 端当前仅接收，不影响） |
| 5 | ~~SPI2 配置漂移~~ | mouse.ioc vs spi.c | ✅ M0 已统一 |
| 6 | ~~校准 Flash 磨损~~ | （旧原型） | ✅ 已移除 |
| 7 | ~~USB 描述符为默认未定制~~ | usbd_hid.c | ✅ M3 复合重做完成 |
| 8 | ~~Enter_Stop_Mode 双初始化~~ | （旧原型） | ✅ 已移除，低功耗另案 |
| 9 | SD 传输速率慢（~4MB/2-3min） | sd_spi.c + usb_storage.c | M4 优化：DMA + CMD18/CMD25 多块传输 |

## 8. 目标架构（升级后分层）

```
┌─────────────────────────────── app ───────────────────────────────┐
│ rf_rx_task │ scsi_msc_task │ sd_log_task │ dbg_task(状态/看门狗)      │
└───────┬────────────────────────────────────────────────────┬──────┘
        │ 只调用 rtos_api 抽象层（可替换内核：FreeRTOS → 自研）      │
┌───────▼────────────────────────────────────────────────────▼──────┐
│ protocol：RF 数据包（结构体序列化/校验）｜ 日志帧格式 ｜ USB 状态机  │
├────────────────────────────────────────────────────────────────────┤
│ drv：nRF24L01(SPI2) ｜ SD(SPI1 阻塞)+diskio ｜ FatFs ｜ USB 复合(HID+MSC)  │
├────────────────────────────────────────────────────────────────────┤
│ hal：CubeMX 外设层（SPI/DMA/TIM/USB 库）＋ 系统 tick（SysTick 单源）│
└────────────────────────────────────────────────────────────────────┘
```

核心设计决策（详见方案 D1~D10）：业务统一经 `rtos_api` 调用内核、SD 走独立 SPI1（当前阻塞式批量传输，DMA 预留）、MSC 与本地日志以"单写者闸门"互斥、SCSI 请求任务化避免长中断。里程碑 M0~M5 及验收标准见[接收端开发方案.md](接收端开发方案.md)。
