# nrfrx-usb 项目架构说明

> 更新：2026-09-05 ｜ 文档状态：随代码演进持续维护（改代码/加模块必须同步更新本文档 + [开发日志.md](开发日志.md)）
> 关联文档：[接收端开发方案.md](接收端开发方案.md)（执行计划/验收）｜ [开发日志.md](开发日志.md)（变更与排障流水）｜ mouse.ioc（CubeMX 工程）

---

## 1. 项目概述

本仓库是 **2.4G 空中鼠标系统的接收端（RX）**：一块 USB 接收器硬件，通过 nRF24L01 接收发射端（TX）发来的鼠标数据包，再以 **USB HID 鼠标**上报给主机。发射端是另一个工程，其主函数参考副本 `txmain.c` 放在本仓库根目录供对照。

**当前定位**：功能原型（能跑通 RX→USB 链路）。
**升级目标（进行中）**：接收端升级为 **USB MSC+HID 复合设备**（同一 USB 口既是鼠标接收器又是 U 盘），并把鼠标原始数据与位移数据通过 **SD 卡（FatFs）** 落盘记录；调度基于 FreeRTOS（后续替换为自研微内核）。升级范围以 [接收端开发方案.md](接收端开发方案.md) 为准。

系统全貌：

```
┌─────────────┐   2.4G (nRF24L01, 2Mbps)   ┌──────────────────────────────┐
│  TX 发射端   │ ─────────────────────────▶ │ RX 接收端（本仓库）            │
│ STM32F103   │  32B 载荷 x/y/按键/gx/gy/gz │ nRF24L01 ─▶ USB HID 上报主机    │
│ MPU6050 姿态│                            │ (SPI2)     └─▶ (目标) SD 卡日志 │
└─────────────┘                            └──────────────────────────────┘
```

## 2. 硬件平台

| 项 | 值 | 说明 |
|---|---|---|
| MCU | STM32F103C8T6 | LQFP48；64KB Flash / 20KB RAM |
| 时钟 | HSE 8MHz，PLL ×9 → 72MHz | USB 时钟 48MHz（PLL/1.5） |
| 调试 | SWD（PA13/PA14） | ST-Link |
| USB | FS 设备（PA11 DM / PA12 DP） | 现为 HID 鼠标类 |
| 2.4G | nRF24L01+ 模块 | SPI2 + CE/CSN/IRQ |
| 姿态 | （RX 板无；TX 板为 MPU6050） | RX main 中有遗留初始化调用，待清理 |
| SD | 已实现（`App/Src/sd_spi.c`）；模块 **VCC=5V**（板载 LDO→3.3V + 电平转换 3.3/5V 兼容） | SPI1 默认映射 PA5/6/7 + CS=PB12；阻塞式 HAL_SPI（批量传输）；DMA 未启用（预留）；用前格式 FAT16/FAT32 |
| OLED | PB10/11 软件 I2C（可转移） | 调试显示 |

## 3. 目录结构

```
nrfrx-usb/
├── Core/                    CubeMX 生成 + 系统入口
│   ├── Inc/  Src/           main.c / stm32f1xx_it.c / gpio / i2c / spi / tim / usart / system
├── App/                    ★ 应用层（原 MDK-ARM/user，2026-09-05 迁出）
│   ├── Src/                 NRF24L01.c / NRF_Demo.c / OLED.c / sd_spi.c / diskio.c / usb_storage.c
│   └── Inc/                 对应头文件（NRF24L01/NRF_Demo/OLED/sd_spi/diskio/usb_storage）
├── Reference/              TX 端参考代码（txmain.c，仅对照不编译）
├── MDK-ARM/
│   ├── freertos/            FreeRTOS 内核源码（tasks/queue/semphr/heap_4…）
│   ├── mouse.uvprojx        Keil 工程（用户代码组 App 指向 App/Src+Inc）
│   └── …                    编译产物不入库
├── Middlewares/ST/STM32_USB_Device_Library/   ST USB 设备库（Core + HID/MSC 类）
├── USB_DEVICE/App  Target/   CubeMX USB 工程文件（usb_device/usbd_desc/usbd_conf）
├── Drivers/  CMSIS/HAL       标准外设库
├── txmain.c                  TX 端主函数参考副本（仅对照，不参与编译）
├── mouse.ioc                 CubeMX 工程
└── 文档：简历.md / 接收端开发方案.md / 项目架构.md / 开发日志.md
```

## 4. 引脚与总线

### 4.1 现状（本仓库实际接线）

| 功能 | 引脚 | 备注 |
|---|---|---|
| 晶振 | PD0/PD1 | HSE |
| SWD | PA13/PA14 | |
| USB | PA11/PA12 | DM/DP |
| nRF | SPI2 = PB13(SCK)/PB14(MISO)/PB15(MOSI)；CE=PB0；CSN=PB1；IRQ=PB5 | |
| OLED | PB10/PB11 | 软件 I2C |
| UART1 | PA9/PA10 | printf 调试（现状闲置，拟保留） |
| GPIO 输入 | PA3/PA4（下拉） | 遗留按键输入，当前无实际用途 |
| LED | PA5 | 调试指示灯 |

### 4.2 目标变更（升级后，详见方案 M0）

| 变更 | 引脚 | 原因 |
|---|---|---|
| SD 新增 | SPI1 默认映射 SCK=PA5/MISO=PA6/MOSI=PA7 + CS=PB12（DMA 预留，当前阻塞式批量传输） | 独立 SPI，避免与 nRF 争用 SPI2（PA5 原 LED 让出） |
| UART1 | 保留（默认） | printf 调试台 |

## 5. 数据流

**现状（RX 主循环）**

```
nRF24L01(RX)  SPI2 轮询 10ms ─▶ MousePacket_t{buttons,x,y,…}
      └─▶ USBD_HID_SendReport(4B: buttons,x,y,0) ─▶ 主机
OLED 定时刷新链路状态
```

**目标（升级后，M1~M4）**

```
nRF IRQ → 信号量 → [rf_rx_task] 收包 → HID 上报任务 → USB 主机
                          │
                          ├─▶ 环形缓冲(RAM 4~8KB) ─▶ [sd_log_task] 批量写 FatFs ─▶ SD(SPI1 阻塞)
USB: 主机 ⇄ USBD(MSC) → SCSI 扇区访问（当前在 ISR 上下文，M3-B 任务化）
      主机枚举占用 SD → 暂停 sd_log_task（单写者闸门）→ 拔出恢复
```

## 6. 现有代码模块要点（用户代码）

| 模块 | 职责 | 关键点 / 问题 |
|---|---|---|
| `Core/Src/main.c` | RX 主循环 | 10ms 轮询收包→HID 上报（M0a 后单 while 结构）；HAL tick 由 SysTick 单源驱动 |
| `stm32f1xx_it.c` | 中断 | SysTick 处理中 **HAL_IncTick 被调两次**；无 EXTI3/4 中断处理器 |
| `App/Src/NRF24L01.c` + `App/Inc/NRF24L01.h` | nRF 驱动 | 寄存器级；固定 32B 载荷；2Mbps/16bit CRC/自动应答重发(10)；TX 发送**阻塞等 IRQ 最长 100ms**；RX 轮询 STATUS；地址/通道写死 |
| `App/Src/NRF_Demo.c` + `App/Inc/NRF_Demo.h` | 收发 Demo | 角色宏 DEMO_ROLE；固定演示包循环；OLED 状态显示；**实际 main 未调用 Run** |
| `App/Src/sd_spi.c` + `App/Inc/sd_spi.h` | SD 卡 SPI 驱动 | SPI1 阻塞式（批量 HAL_SPI）；CMD0/CMD8/ACMD41/CMD58 初始化；CMD17/CMD24 单块读写；CMD9 CSD 容量解析；18MHz 数据传输 |
| `App/Src/diskio.c` + `App/Inc/diskio.h` | FatFs 磁盘 I/O 适配 | 卷 0=SD（接 sd_spi）；单/多扇区读写；ioctl 仅 CTRL_SYNC/GET_BLOCK_SIZE |
| `App/Src/usb_storage.c` + `App/Inc/usb_storage.h` | USB MSC 存储后端 | SCSI 回调（IsReady/Read/Write/GetCapacity/Inquiry）→ sd_spi；LUN0=SD 卡；开机预缓存容量 |
| USB HID | ST 库默认 | 默认鼠标报告描述符(74B/4B 报告)，未定制 |

## 7. 已知问题清单（评审遗留，处理归属里程碑）

| # | 问题 | 位置 | 处理 |
|---|---|---|---|
| 1 | HAL tick ≈ 2.1× 真实速率（SysTick 双调 + TIM2 再调） | stm32f1xx_it.c:172-183；main.c:209 | M0 单源化 |
| 2 | RX main 死代码/第二个 while 不可达 | Core/Src/main.c:125-160 | M0 清理 |
| 3 | EXTI3/4 回调不可达（无 IRQ Handler） | stm32f1xx_it.c | M0 核实清理 |
| 4 | NRF 发送阻塞最长 100ms | user/NRF24L01.c:150-178 | M1/M3 事件化 |
| 5 | SPI2 配置漂移：.ioc=256 分频，spi.c=16 分频 | mouse.ioc vs Core/Src/spi.c:47 | M0 统一 |
| 6 | 校准 Flash 磨损/加载未生效（属 TX/旧原型） | （旧原型，已移除） | RX 重构剔除 |
| 7 | USB 描述符为默认未定制 | Middlewares/.../usbd_hid.c | M3 复合重做 |
| 8 | Enter_Stop_Mode 双初始化/忙等/唤醒源缺失 | （旧原型代码已移除，低功耗另案） | RX 低功耗另案 |

## 8. 目标架构（升级后分层）

```
┌─────────────────────────────── app ───────────────────────────────┐
│ rf_rx_task │ scsi_task │ sd_log_task │ dbg_task(状态/看门狗)      │
└───────┬────────────────────────────────────────────────────┬──────┘
        │ 只调用 rtos_api 抽象层（可替换内核：FreeRTOS → 自研）      │
┌───────▼────────────────────────────────────────────────────▼──────┐
│ protocol：RF 数据包（结构体序列化/校验）｜ 日志帧格式 ｜ USB 状态机  │
├────────────────────────────────────────────────────────────────────┤
│ drv：nRF24L01(SPI2) ｜ SD(SPI1 阻塞)+diskio ｜ FatFs ｜ USB HID/MSC  │
├────────────────────────────────────────────────────────────────────┤
│ hal：CubeMX 外设层（SPI/DMA/TIM/USB 库）＋ 系统 tick（SysTick 单源）│
└────────────────────────────────────────────────────────────────────┘
```

核心设计决策（详见方案 D1~D10）：业务统一经 `rtos_api` 调用内核、SD 走独立 SPI1（当前阻塞式批量传输，DMA 预留）、MSC 与本地日志以"单写者闸门"互斥、SCSI 请求任务化避免长中断。里程碑 M0~M5 及验收标准见[接收端开发方案.md](接收端开发方案.md)。
