# 开发日志（nrfrx-usb）

> 目的：记录本项目每一次代码/方案改动、遇到的 Bug 与处理方式，供回溯与复盘。
> 维护规则（请一直遵守）：
> 1. **新条目写在最上方**（最新在上），一条一个主题；
> 2. 每条包含：日期｜范围（改了什么文件/模块）｜问题现象｜定位/原因｜处理方式｜验证结果｜状态；
> 3. 状态：`待办 / 进行中 / 已解决`；解决后回填验证结果；
> 4. 动代码必须同步更新：[项目架构.md](项目架构.md)（结构/数据流/引脚）、本文件；方案级变更更新[接收端开发方案.md](接收端开发方案.md)；
> 5. 硬件/环境类异常也记录（注明非代码问题）。

## Git 版本管理约定（自 2026-09-05 起）

- 分支：`main` 只放验收通过的基线；里程碑工作直接在 main 上分步提交并打 tag（`m0`…`m5`），需要实验时开 `dev/<里程碑>` 分支
- 提交时机：每个可编译/可验收节点一次提交；每个里程碑结束打 tag；大改前先提交 checkpoint
- 回退：`git reset --hard <tag>`（会丢弃未提交改动，回退前先 `git stash`）
- 提交信息：`<type>: <简述>`（feat/fix/refactor/docs/chore），改动同步记录在本文件
- 入库范围：仅工程代码与文档；编译产物、IDE 个人配置、个人简历/硬件手册、`参考历程/` 一律忽略（见 `.gitignore`）

### 2026-09-06 M4a 实测：系统监控上线；IWDG 暂禁（本板 LSI 异常）
- BOOT 标记定位：启动曾卡死 → 根源在 sysmon_init_hw 的 IWDG/LSI 路径；**禁用 IWDG 后启动正常**（疑似本板 LSI 未真正起振或寄存器等待死锁）
- 实测（稳定供电、USB-only）：鼠标正常；CPU 统计 IDLE 98% / rf<1% / sysmon<1%；栈高水位：rf_rx 余~106/160、sd_log ~122/220、sysmon ~104/300；link_loss_ratio≈94%（TX 事件驱动发包，属正常）
- 处置：IWDG 保留代码但默认关闭（s_iwdg_on=0），喂狗钩子仍在；**待实测 LSI 频率**（建议 MCO 输出 LSI 用频率计/示波器，或换保守重载）后再启用
- 状态：M4 主体（监控/统计/链路计数/栈水位）已上线；剩余：24h 长跑、功耗电流量化、（可选）看门狗按实测频率重开

### 2026-09-06 M4a：系统监控与看门狗（stable 主线）
- IWDG（寄存器直操作，无 HAL 依赖）：LSI/128≈312Hz，RLR=2000 → 溢出 ~6.4s；上电即启动，空闲钩子 + sysmon 任务喂狗（FreeRTOSConfig configUSE_IDLE_HOOK=1）
- 新增 App/Src/sysmon.c/.h：监控任务(prio1)每秒打印链路计数（rx_ok/rx_idle/丢包率近似），每 10s 打印 FreeRTOS 运行时间统计(CPU%)，每 20s 打印 vTaskList 栈水位；console 新增 dbg_puts 大段输出
- rf 任务接入 sysmon_rx_ok/idle 计数
- 编译 0 Error；待硬件：烧录观察 [MON] 行、长跑 24h、电流表量化功耗
- 注：若想换回 HAL 版 IWDG，需 F1 标准包 Drivers/STM32F1xx_HAL_Driver 下 stm32f1xx_hal_iwdg.{h,c} + hal_conf 开 HAL_IWDG_MODULE_ENABLED

### 2026-09-06 MSC/U盘 深挖结论（M3 相关，正式挂起）
- 分支 test/msc-only 完成关键二分：
  1. 纯 MSC（无 HID）可正常枚举、GET_MAX_LUN 应答、完成 INQUIRY 数据+CSW 全握手（EP 需用 2/0x82-0x02，EP1 OUT 会 STALL）
  2. 卡点收敛：**只有"配置时布的第一条 CBW"能被收下；CSW 后重新布的 OUT 永远收不到第二条命令** → 主机超时 → 复位 → 重来，最终 0xA
- 判断：协议本身标准、ST 类代码成熟；问题指向"本工程 Core(v2.0_Cube，CubeMX 生成) 与所配 MSC 类/回调胶水在 F103 上收第二条 CBW 的边界"（疑 ISR 内 CSW 后重布 OUT 与 IN 完成时序/核心回调契约）
- 后续候选（任选再战）：① 整体替换新版多类 USB 中间件（Core+HID+MSC 同代）；② USBPcap/换主机系统对比；③ 在 ISR 里对"CSW 完成后"显式 Flush+PrepareReceive 二次验证
- 状态：挂起；稳定版 main(stable-hid-sd) 不受影响

### 2026-09-06 会话交接摘要（上下文压缩存档，后续据此恢复）
#### Git 与版本
- 远程：github.com/bhxw/2.4g-mouse-and-usb-flash-drive；本地默认分支 main
- tags：v0-baseline / m0a / m0b / m1 / m2a / m2b / m2 / m3? 无 / stable-hid-sd
- 分支：feature/usb-composite（M3 全部 WIP 归档处）
- 提交习惯：每里程碑/修复一提交；`bash tools/keil_build.sh [rebuild]` 自编译（UV4 命令行，需关 Keil GUI）

#### 工程状态速览
- 芯片 STM32F103C8T6；RX 接收端（nRF24L01 SPI2 PB13-15 + PB0/1/5；SD 走 SPI1 默认映射 PA5-7 + CS=PB12；USB PA11/12；UART1 调试；OLED 软I2C PB10/11）
- 结构：App/Src+Inc 用户代码（NRF24L01/NRF_Demo/OLED/rtos_api/app_main/sd_log/diskio/sd_spi 等）；Core CubeMX；MDK-ARM/mouse.uvprojx；Middlewares/Third_Party/FatFs(R0.16)
- FreeRTOS 任务：rf_rx(prio5,10ms 收包→HID+写日志)、sd_log(prio3, 队列64→DATA.LOG 追加,512B 缓冲,USB 枚举期间暂停)；rtos_api 抽象层
- **main = 稳定版 stable-hid-sd：纯 HID 鼠标 + SD 日志（硬件验证通过）**；SD 卡需 5V 供电，FAT32

#### 关键经验/踩坑（已修复并验证）
1. HAL tick 双计数 → SysTick 单源；TIM2 hack 移除；TIM3=1ms 运行统计
2. LogRecord 结构体 4 字节对齐 → 加 `#pragma pack(1)` + 16B 断言（否则 PC 解析 ts 全假）
3. FatFs 用官方 R0.16（源放 Middlewares/Third_Party/FatFs），ffconf 裁剪；例程 0.08A/STIO 版不可用
4. nRF 冷启动误报 ERR → 检测前延时 300ms + 重试 3 次
5. USBD_static_malloc 原只容 HID 且恒返同指针 → 改双缓冲(小64/HID、大1024/MSC)复用（MSC 二次 Init 失败根因）
6. 供电：SD 模块 VCC 必须接 5V，勿用 3.3V；串口只接 TX/RX/GND；USB-only 供电可行（m2 验证）

#### M3 复合设备（USB MSC+HID，WIP，见分支 feature/usb-composite）
- 现象：Windows 端 MSC 枚举到"单条 INQUIRY(字节/CSW 均正确)→主机复位→代码10"，永不进入第二条命令；HID 正常
- 已做：旧版单类 Core 上手写复合类包装器（App/Src/usbd_composite.c）路由 Setup/DataIn/Out；MSC 端点改 0x82/0x02；句柄静态化；容量开机预缓存；诊断打印已内建（[CMP]/[MSC]/[INQ]/[CSW]/[CAP]）
- 疑点：老版 Core+老 MSC 在复合枚举下与 Windows 握手不兼容；下一候选方案：整体替换新版多类 USB 中间件，或换主机/USBPcap 分析
- 复现路径：切 feature/usb-composite → 编译烧录 → 插 USB（串口看 [CMP] 行）→ 设备管理器无盘符/代码10

#### 简历/文档
- 简历.md 为秋招投递版（M3 表述需注意：复合设备未完成，避免过度宣称）；参考 开发日志 各条目核对安全线
- 文档：接收端开发方案.md（D1~D10/M0~M5）、项目架构.md、开发日志.md、README.md

#### 环境
- python3 占位符问题已修（复制 python3.exe）；GBK 源文件读取需 iconv；Keil 命令行编译脚本 tools/keil_build.sh；用户级 skill keil-build

### 2026-09-06 决策：M3 复合设备挂起，main 恢复 HID+SD 稳定版
- 结论：MSC+HID 复合在 Windows 上枚举卡在"单条 INQUIRY 后主机复位/代码10"，固件侧字节/CSW 均已验证正确；老版单类 Core + 手写复合 + 老版 MSC 组合疑与 Windows 枚举器握手不兼容，继续盲调收益低
- 处置：当前 M3 全部工作（含 allocator/路由/诊断）归档到分支 **feature/usb-composite**；main 回退为 纯 HID + SD 日志（M2 等价稳定行为），打 tag **stable-hid-sd**
- 待续：换新版本多类 USB 中间件整体替换 或 换主机/协议级工具（USBPcap）再攻 M3
- 状态：main 稳定可用；M3 WIP 在分支

### 2026-09-05 M3a：USB MSC+HID 复合设备（编译通过，待硬件枚举验证）
- 端点修正：老版 MSC 默认 EP IN 0x81 与 HID 冲突 → 改 0x82(IN)/0x02(OUT)
- 新增：`App/Inc/usbd_composite.h`+`App/Src/usbd_composite.c`——单类 Core 上的复合包装器（配置描述符合并两接口：iface0=HID(EP1 IN 0x81)，iface1=MSC(EP2 OUT/IN)；Setup 按 wIndex 路由、DataIn/Out 按 EP 路由）
- 新增：`App/Inc/usb_storage.h`+`App/Src/usb_storage.c`——MSC 介质层（LUN0=整张 SD，INQUIRY/Capacity(CSD)/Read/Write），并记录最近介质访问时刻供 sd_log 门控
- sd_spi 增 `SD_GetBlockCount`（CMD9 CSD v1/v2 解析）；usb_device.c 改注册 `USBD_Composite`
- sd_log：修复缓冲 512B 越界隐患 + USB 占用时暂停落盘、满则丢新计 dropped（单写者互斥 v1）
- 说明：MSC 扇区访问在 USB ISR 内完成（M3a 方案 A）；后续 M3c 再任务化
- 状态：编译 0 Error/1 Warning（NRF_Demo 未用变量，遗留）；**待硬件枚举验证：设备管理器应出现“鼠标 + U盘”**

### 2026-09-05 fix(log): LogRecord 对齐 20B 导致解析错位（已解决，硬件复测通过）
- 现象：PC 解析 ts 出现上亿"假时间戳"（926M/181M/32M ms…），板上 tick 打印正常
- 定位：ARMCC 默认 4 字节对齐，`magic(u16)+2填充+ts(u32)` → 记录实际 **20B**，解析脚本按 16B 从偏移 2 读 ts 拼出假值（hex 证实：`524d | a5a5 | 3b040000`，真 ts=1083）
- 处理：`log_format.h` 加 `#pragma pack(1)` + 16B 编译期断言
- 验证：硬件复测 `750 rec, 1003→68883ms, 11 rec/s`，hex 正常 → **M2 硬件链路闭环**
- 另：NRF 冷启动 ERR 已修复（检测前延时 300ms + 重试 3 次）

### 2026-09-05 M2c：日志链路（队列→批量写 DATA.LOG + PC 解析，待编译）
- rtos_api 扩展：队列 create/send/recv（FreeRTOS 队列，RTOS_WAIT_FOREVER 支持）
- 新增：`App/Inc/log_format.h`（16B 定长记录 v1）、`App/Inc/sd_log.h`+`App/Src/sd_log.c`（挂载→DATA.LOG 追加→512B 缓冲批量写→1s f_sync；写失败自动卸载重试）
- 改动：app_main 的 rf_rx 收包后调用 `sd_log_write_packet`；启动 sd_log 任务；删除 sd_probe（功能并入 sd_log）
- 新增：`tools/parse_log.py`（记录数/时间跨度/速率统计，--csv 导出）
- 决策记录：M2b 的"扇区回环自检"让位于真实日志链路；SD 硬件首测预期 UART：`[LOG] DATA.LOG open (append)` → `[LOG] flushed rec_total=…`
- 状态：已编译通过（无报错），tag `m2` 已打；功能验证待 SD 硬件实测

### 2026-09-05 M2b：FatFs 接入 + diskio 对接 + UART 自检（待编译）
- 来源决策：官方 elm-chan 与 raw.githubusercontent 在本机网络不可达；改从 **ST stm32-mw-fatfs（GitHub 子模块）** 稀疏拉取 R0.15 内核
- 拷贝：`Middlewares/Third_Party/FatFs/src/{ff.c,ff.h,integer.h,ffconf.h}` + `LICENSE.md`（去除 ST 通用驱动壳 diskio/ff_gen_drv/drivers，保留经典 ChaN 结构）
- 新增：`App/Src/diskio.c` + `App/Inc/diskio.h`（卷0=SD，接 sd_spi；单/多扇区读写；ioctl 仅 CTRL_SYNC/GET_BLOCK_SIZE）
- 新增：`App/Inc/console.h` + `App/Src/console.c`（UART1 dbg_printf，无需 C 库重定向）
- 新增：`App/Inc/sd_probe.h` + `App/Src/sd_probe.c`（自检任务：f_mount→写 TEST.LOG 512B→读回 memcmp 校验→UART 报 OK/FAIL，3s 重试）
- 配置：ffconf.h 裁剪（LFN/长名关、CP437、VOLUMES=1、NORTC、512 扇区、仅读写）
- 改动：app_main 启动 sd_probe 任务；uvprojx 新增 FatFs 组 + include 路径
- 状态：已编译通过（无报错，用户确认），tag `m2b` 已打；功能验证待 SD 硬件实测（PA5-7+PB12、5V、FAT32、UART1）

### 2026-09-05 M2a：SD 底层驱动（SPI1+CS=PB12，草稿待编译）
- 新增：`App/Inc/sd_spi.h` + `App/Src/sd_spi.c`
- 实现：CS=PB12；CMD0→CMD8→ACMD41(HCS)→CMD58(OCR) 判定 SDHC 块寻址；CMD17 读块 / CMD24 写块（0xFE 令牌、数据应答 0x05、忙等待）；初始化 281kHz→数据 18MHz 变速
- 决策记录：**首版用阻塞 HAL_SPI_TransmitReceive**（先保证可用）；DMA 句柄已由 CubeMX 就绪（DMA1_CH2/CH3），吞吐不足时再切（方案 D3 备注）
- 时序蓝本：`参考历程/SD/utility/Sd2Card.cpp`
- 状态：待 Keil 编译验证；功能验证待 SD 硬件接线 + FatFs（M2b）提供读回手段

### 2026-09-05 M1a：RTOS 抽象层 + 任务化骨架（FreeRTOS 起步；待 Keil 编译后 tag m1）
- 新增：`App/Inc/rtos_api.h` + `App/Src/rtos_api.c`（D2 抽象层：业务只经 rtos_api，M5 换自研内核仅改此实现）
- 新增：`App/Inc/app_main.h` + `App/Src/app_main.c`（rf_rx 任务：10ms 轮询收包→HID 上报）
- 改动：main.c 初始化后调用 `app_start()`（内部启动调度器，不返回），删除裸机主循环；uvprojx App 组 +4 条目
- 调整：`configTOTAL_HEAP_SIZE` 4096→10240（多任务+TCB+运行统计余量）
- 说明：调度器启动后 SysTick 经 it.c 喂 FreeRTOS；TIM3(1ms) 作运行统计源（mydebug=1 时调度器自动调用 tim.c 的 vConfigureTimerForRunTimeStats）
- 状态：已编译通过（用户确认无报错），tag `m1` 已打（回退安全点 tag `m0b`）

### 2026-09-05 M0 定版修正（CubeMX 再生成后复核；SPI1 方案变更，待编译验收后 tag m0b）
- 硬件/工程变化（用户在 CubeMX 完成）：SPI1 用默认映射 **PA5/6/7**（非 remap）、nRF 保持 **PB0/1/5（IRQ=PB5 不变）**、删除 TIM2、移除 I2C1 与 MPU6050、TIM3 配 1ms、SPI1 DMA1_CH2/CH3 使能
- 复核结论：M0a 的 SysTick 单源、单主循环、USER 区改动在 CubeMX 再生成后**全部保留**
- 修正动作：
  1. 撤销 M0b-prep 的 IRQ→PA8 宏，改回 PB5，注释定版
  2. uvprojx 移除 freertostask.c/.h 条目（其依赖的 I2C/MPU6050 已删，属旧原型非业务代码）
  3. `ulGetRunTimeCounterValue` 自 freertostask.c 迁移至 main.c（FreeRTOSConfig `mydebug=1` 需要该符号）
  4. 删除死文件：App 下 MPU6050.c/.h/MPU6050_Reg.h、freertostask.c/.h（git 历史可恢复）
  5. 方案/架构/README 引脚与模块信息同步为最终版（SPI1=PA5-7、IRQ=PB5、无 NOJTAG）
- 状态：待 Keil 编译通过后打 tag `m0b`

### 2026-09-05 M0b-准备：nRF IRQ 引脚宏 PB5→PA8（配合 SPI1_MOSI 复用，待 CubeMX/接线）
- 范围：`App/Inc/NRF24L01.h`
- 处理：IRQ 宏改 PA8（PB5 让给 SPI1_MOSI）；宏注释同步
- 待办：CubeMX 侧增加 SPI1 remap(PB3/4/5)+DMA1 CH2/CH3、删除 TIM2、TIM3 周期改 1ms、SPI2 分频与代码对齐（16）→ 见主回复的 GUI 步骤
- 状态：待用户在 CubeMX 执行后复核

### 2026-09-05 M0a：时间基准单源化 + RX 主循环重构（已提交 tag m0a）
- 范围：`Core/Src/main.c`、`Core/Src/stm32f1xx_it.c`
- 问题：HAL tick ≈2.1× 真实速率（SysTick 双 `HAL_IncTick` + TIM2 再叠加）；main 存在不可达死代码（第二个 while、`freertos_task_start()`、MPU6050/OLED 残留）
- 处理：
  1. `SysTick_Handler` 仅保留一处 `HAL_IncTick`（SysTick 单源）
  2. 移除 TIM2 tick hack：不再 `HAL_TIM_Base_Start_IT(&htim2)`，回调删除 TIM2 分支，仅留 TIM3 做运行时间统计计数
  3. 主循环重构：收包→HID 上报的 10ms 轮询移入唯一 `while(1)`，删除不可达代码与无用初始化；`g_ulRunTimeCounter` 定义保留
- 待办：M0b 从 .ioc 彻底移除 TIM2、配置 SPI1/DMA（见下条）；TIM3 周期在 M0b 调为 1ms
- 验证：需 Keil 重新编译一次确认（本环境无法编译）
- 状态：已解决（待编译确认）

### 2026-09-05 仓库初始化与目录整理
- 范围：删除误带的 TX 工程 .git 历史 → 重建干净仓库；目录整理；推送远程 `github.com/bhxw/2.4g-mouse-and-usb-flash-drive`
- 目录变更：`MDK-ARM/user/`（用户代码）迁至 **`App/Src` + `App/Inc`**；`txmain.c` 迁至 `Reference/`；新增 `README.md`、`.gitignore`
- 同步处理：`mouse.uvprojx` 的 Group 名 user→App、12 条 FilePath、IncludePath 全部改写并经 XML 校验；Keil 中需重新加载工程
- 入库边界：工程代码 + 文档入库；**个人简历/硬件手册/参考历程 不入库**（隐私与体积考虑，仍留本地）
- 基线：首个提交 + tag `v0-baseline`
- 2026-09-05 补充：应隐私要求，`.gitignore` 简历匹配改为匿名通配（`*简历*.docx`），已重建单提交历史并强推远程，旧含姓名记录清除
- 状态：已解决

## 模板（复制使用）

```
### YYYY-MM-DD 标题
- 范围：
- 问题现象：
- 定位/原因：
- 处理方式：
- 验证结果：
- 状态：
```

---

### 2026-09-05 引脚/硬件与参考例程确认（M0 前置，已收口）
- 范围：打开项确认；参考代码登记（新增目录 `参考历程/`）
- 结论：
  1. PA8（nRF IRQ 迁移目标）、PB12（SD CS）：`.ioc` 均未占用，空闲确认（xlsx 为数字映射表，M0 接线前再人工核对一次）
  2. SD 模块（MicroSD Card Adapter 用户文档）：**VCC 接 5V**（板载 LDO→3.3V + 电平转换 3.3/5V），SPI 六线（GND/VCC/MISO/MOSI/SCK/CS），支持 SD≤2G / SDHC≤32G，使用前格式化 FAT16/FAT32
  3. UART1 保留作 printf 调试
  4. 参考例程甄别结果：
     - `FATFS V0.08A-SD Card` 与 `USB-Mass_Storage-SD Card` 的介质层均为 **SDIO（sdio_sd.c）——C8T6 无 SDIO 外设，仅作 diskio/SCSI/介质层概念参考，不可直接移植**
     - SPI 模式 SD 初始化与单/多块读写时序以 **`参考历程/SD/utility/Sd2Card.cpp`**（Arduino 库）为逻辑蓝本，改写为 HAL SPI1+DMA
     - nRF 对照 `参考历程/nrfSTM32程序/HARDWARE/NRF24L01`（标准库寄存器版，即现驱动移植来源）
     - FatFs 采用新版本（R0.15），不复用例程内 0.08A
- 影响：M2 自写 SPI-SD diskio；M3 使用本项目 CubeMX USB 设备库（usbd_msc），旧 usb-lib 例程仅对照概念
- 状态：已确认（待 M0 执行）

### 2026-09-05 建立开发文档体系
- 范围：新增 `项目架构.md`、`开发日志.md`、`接收端开发方案.md`；更新 `简历.md`（秋招投递版）
- 背景：开始接收端升级（USB MSC+HID 复合 + SD 日志）前，需要架构文档、执行方案与变更流水
- 处理：架构/数据流/引脚写入 `项目架构.md`；执行计划与验收写入 `接收端开发方案.md`（决策 D1~D10、里程碑 M0~M5）；简历按方案改写为投递版
- 状态：已解决

### 2026-09-05 代码评审（读码发现，待 M0 起逐项修复）
- 范围：全仓库用户代码（Core/Src、MDK-ARM/user、USB 层）通读
- 定位/原因与处理计划：

| 现象 | 定位 | 处理（里程碑） |
|---|---|---|
| HAL tick ≈2.1× 真实速率，一切延时/超时/采样周期失真 | SysTick_Handler 中 `HAL_IncTick()` 被调两次（stm32f1xx_it.c:175 与 177）；main.c 又用 TIM2 中断调 `HAL_IncTick` | M0：SysTick 单源，去 TIM2 hack |
| RX main 结构错乱：第一个 while(1) 后全是不可达死代码 | Core/Src/main.c:125-160，含 `freertos_task_start()` 注释、第二个 while | M0：清理重构 |
| 按键 EXTI 回调不可达 | 工程无 EXTI3/4 中断处理器，PA3/PA4 在 .ioc 是普通 GPIO_Input | M0：核实用途后清理 |
| nRF 发送阻塞最长 100ms | NRF24L01_TxPacket 轮询 IRQ + 超时 | M1/M3：改中断+任务/信号量 |
| SPI 配置漂移：.ioc=256 分频(140kHz)，spi.c 实际=16 分频 | mouse.ioc vs Core/Src/spi.c:47 | M0：统一，按目标速率定 |
| 零偏校准 Flash 每 ~2s 擦写一次（磨损）；Load 未写回运行偏移 | user/freertostask.c 校准任务/载入函数 | RX 无 MPU6050，此模块整体剔除（属旧原型） |
| USB 报告描述符为 ST 默认，未定制 | Middlewares usbd_hid.c HID_MOUSE_ReportDesc | M3：复合设备时重做 |
| Enter_Stop_Mode 唤醒后 `MX_USB_DEVICE_Init` 调两次 + 3s 忙等 + 无明确唤醒源 | user/freertostask.c:76-111 | RX 低功耗另立方案（不在当前 RX 里程碑） |
| 简历"互斥量消除界面卡顿"与代码不符（全工程无 Mutex） | —— | 简历已删该表述；如需保留需先实现 |

- 状态：已解决（记录完成）；代码修复在 M0 起执行

### 2026-09-05 接收端升级方案确认（决策）
- 内容：确认 D1~D10，写入 `接收端开发方案.md`
  - D1 调度先 FreeRTOS，后自研微内核（M5）；D2 业务只经 `rtos_api` 抽象层；
  - D3 SD 走 SPI1 重映射 PB3/4/5 + CS=PB12 + DMA1_CH2/CH3（需 NOJTAG）；
  - D4 nRF 留 SPI2，IRQ PB5→PA8；D5 SCSI 先 A（ISR 内联）后 B（任务化）；
  - D6 SD 单写者闸门（主机占用暂停日志）；D7 日志定长二进制+PC 解析；
  - D8 时间基准单源化；D9 保留 UART1 调试；D10 TX 算法优化后置，绝对坐标宏否决
- 打开项（执行前需确认）：PA8/PB12 引脚可用性、SD 卡座 3.3V/规格、UART1 是否保留、日志格式确认
- 状态：进行中（待用户确认打开项后执行 M0）

### 2026-09-04 环境与工具修复（非代码）
- 现象：`python3` 命令静默失败（退出码 49）；`freertostask.c` 为 GBK 编码无法直接读
- 原因：Windows 商店 python3 存根抢占 PATH；文件编码非 UTF-8
- 处理：复制真实 Python313 的 python.exe 为 python3.exe（PATH 顺序在存根之前）；GBK 文件经 iconv 转 UTF-8 读取
- 状态：已解决
