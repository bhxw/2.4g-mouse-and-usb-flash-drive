# RX 无线接收器升级方案（v2 — 已确认决策版）

> 项目背景：2.4G 空中鼠标（STM32F103C8 + MPU6050 + nRF24L01 + USB HID）的接收端升级。
> 目标：接收端变为 **USB MSC+HID 复合设备**（既是鼠标接收器又是 U 盘），同时把鼠标原始/位移数据**写入 SD 卡**；
> 调度先基于 **FreeRTOS** 落地，稳定后按 M5 替换为**自研微内核**。发送端（TX）算法优化排在接收端完成之后。
> 本文件为执行与验收的唯一依据；状态在执行过程中逐项更新。

---

## 0. 已确认的决策

| # | 决策 | 结论 |
|---|---|---|
| D1 | 调度基座 | 先用现成 FreeRTOS（工程内已有源码/移植），M5 再替换为自研微内核 |
| D2 | 应用与内核解耦 | 全部任务只经自定义 `rtos_api` 薄层调用内核（创建/延时/信号量/互斥/队列），保证 M5 可无缝替换 |
| D3 | SD 物理接口 | **SPI1 默认映射（PA5/PA6/PA7）** + 独立 CS；当前阻塞式批量传输，DMA1_CH2(RX)/CH3(TX) 预留；PA5 原 LED 让出 |
| D4 | nRF 不变 | 留在 SPI2（PB13/14/15）+ CE=PB0 + CSN=PB1；IRQ=PB5（不变） |
| D5 | SCSI 处理 | **M3 先 A（USB ISR 内联阻塞 SPI）跑通，再移植为 B（请求投递 scsi_task 任务化）**；任务化后信号量/互斥真正承重 |
| D6 | SD 并发模型 | **单写者闸门**：主机枚举占用 SD（MSC）期间暂停本地日志，拔出恢复；不在 ISR 里取阻塞锁 |
| D7 | 日志格式 | 定长二进制块 + PC(Python) 解析脚本（默认；脚本同时作为后续 TX 算法标定分析工具） |
| D8 | 时间基准 | SysTick 单源修正：删 TIM2 tick hack、删 `SysTick_Handler` 重复 `HAL_IncTick`；TIM3 改 1ms 中断做 run-time stats 计数源 |
| D9 | UART1 | 保留作 printf 调试台（建议至少保留到 M4，之后可删） |
| D10 | TX | 本轮不做；后续：算法优化 + 日志数据离线标定分析（绝对坐标宏方案已否决，备选：演示翻页/DPI 档位，待定） |

## 1. 引脚方案（待执行前最终确认）

| 信号 | 引脚 | 变更 |
|---|---|---|
| SD | SPI1 默认映射：SCK=PA5 / MISO=PA6 / MOSI=PA7 | 新占用（PA5 原 LED 让出） |
| SD CS | **PB12**（空闲） | 新占用，推挽输出 |
| nRF IRQ | PB5（不变） | EXTI 输入 |
| nRF 数据 | SPI2 PB13/14/15、CE=PB0、CSN=PB1 | 不动 |
| OLED | PB10/11（软件 I2C） | 不动 |
| USB | PA11/PA12 | 不动 |
| SWD | PA13/PA14 | 不动 |
| 晶振 | PD0/PD1 | 不动 |
| UART1 | PA9/PA10 | 保留作调试（D9） |

已确认：PB12（SD CS）空闲；PA8 无需占用（nRF IRQ 保持 PB5）；SD 模块 VCC=5V（板载 LDO→3.3V + 电平转换），SPI 六线，支持 SD≤2G/SDHC≤32G，格式化 FAT16/FAT32。

### 1.1 参考例程索引（`参考历程/`，取逻辑不搬库）

| 例程 | 内容 | 用途 |
|---|---|---|
| `SD/utility/Sd2Card.cpp/h` | Arduino SPI-SD 驱动（初始化时序/单多块读写） | ★ **SPI-SD 底层逻辑蓝本** → 已改写为 HAL SPI1 阻塞式批量传输 |
| `FATFS V0.08A-SD Card` | FatFs + diskio 例程 | diskio 对接模式参考；**介质为 SDIO，C8T6 无 SDIO 不可移植**；FatFs 用新版 R0.15 |
| `USB-Mass_Storage-SD Card` | 旧 usb-lib MSC 例程（mass_mal/memory/scsi） | MSC 介质层与 BOT 概念对照；本项目走 CubeMX usbd_msc，架构不同 |
| `nrfSTM32程序/HARDWARE/NRF24L01` | 标准库寄存器版 nRF 驱动 | 现 HAL 驱动的移植来源，比对用 |
| `SPI Flash(...)` / `USB-Mass_Storage-* Flash` | SPI Flash 与 MSC 变体 | SPI/DMA 与 LUN 实现备查 |

## 2. 里程碑

### M0 — 引脚与时间基准重构（已基本完成，待编译验收）
- CubeMX：SPI1 默认映射 PA5/6/7 + DMA（CH2/CH3）✅（DMA 已配置但软件未启用，当前阻塞式传输）、删 TIM2/I2C1/MPU6050 ✅；SD CS=PB12 由驱动管理（M2）
- 修正时间基准（D8）：SysTick 单源、删死代码（RX main 第二个 while、`data[10]`、重复 EXTI callback 等）
- 验收：SWD 烧录/调试正常；`HAL_GetTick` 秒表对表误差 <1ms；FreeRTOS tick 正常

### M1 — FreeRTOS 任务化骨架 + rtos_api（1~2 天）
- 任务集收敛：`rf_rx_task`（nRF IRQ 信号量唤醒→收包→HID 上报）、`sd_log_task`（消费环形缓冲刷卡）、`scsi_task`（M3-B 用，先占位）、`dbg_task`（状态/LED/printf）
- 建立 `rtos_api` 抽象层（D2），所有任务只调它
- 验收：骨架空转稳定 24h 无溢出；任务状态可观测

### M2 — SD 底层 + FatFs 日志（1~2 天）
- SPI1 SD 驱动（`sd_spi.c`：CMD0/CMD8/ACMD41/CMD58 初始化，CMD17/CMD24 单块读写，CMD9 CSD 容量解析；阻塞式批量 HAL_SPI，18MHz 数据传输）；`diskio.c` 对接 FatFs（R0.16，关 LFN、单文件、单扇区缓存）
- 日志链路：数据 → RAM 环形缓冲（4~8KB）→ `sd_log_task` 批量写（D7 格式）
- 验收：格式化→连续写 1MB→回读一致；拔卡/写满不崩；掉电复位后文件可读
- 状态：SD 驱动 + diskio 已实现（分支 `test/msc-only` 验证中）；DMA 优化预留

### M3 — USB 复合设备 MSC+HID（2~4 天）
- 描述符：配置描述符挂 HID 接口 + MSC 接口；HID/MSC 双类注册与接口/端点路由
- **M3-A（内联）**：SCSI 在 USB ISR 内阻塞式 SPI 服务；主机在线暂停日志（D6）
  - 实现：分支 `feature/composite-hid-msc`，复合类包装器 `usbd_composite.c` 路由 HID/MSC 回调；MSC 端点改至 EP2(0x82/0x02) 避免与 HID EP1 冲突；PID 0x5741
  - 端点分配：EP0 控制(64B) | EP1 IN HID 中断(4B) | EP2 OUT/IN MSC 批量(64B)；PMA 288B/512B
  - ✅ 编译通过 + **硬件验证通过**
- **M3-B（任务化移植）**：storage 回调 → 请求+信号量唤醒 `scsi_task` → 完成后回填续跑 BOT 状态机（改 `usbd_msc.c` 调用点）
  - ✅ 已实现：`SCSI_ProcessRead/Write` 在 USB ISR 中设置标志 + `rtos_queue_send_from_isr` 信号 `scsi_msc_task`，任务执行 SD 操作后调用 `USBD_LL_Transmit/PrepareReceive`/`MSC_BOT_SendCSW` 续传 BOT 状态机；初始化顺序修正（SD preinit 在 USB 启动前）
  - ✅ USB ISR 优先级修正为 (5,0) 兼容 FreeRTOS `configMAX_SYSCALL_INTERRUPT_PRIORITY`；SCSI_Read10 双更新 bug 修复
  - **硬件验证结果（2026-09-06）**：
    - 鼠标正常移动（nRF→HID 链路正常）
    - U盘 Windows 正常识别，盘符出现 0~10s
    - 文件拷贝功能正确（~4MB 约 2-3 分钟，受 SPI 阻塞式传输限制）
    - 热插拔基本稳定
  - 验收：A 全部用例 + HID 报告周期在 SCSI 服务期间无抖动（USB 抓包量化）
  - **状态：✅ M3 核心功能验证通过，已归档**

### M4 — 可靠性收尾（1~2 天）
- IWDG 看门狗（空闲任务喂）+ 栈高水位监控；丢包/误码计数；功耗（空闲/工作）电流表量化
- 验收：连续运行 24h 日志完整、拷贝校验一致、无复位

### M5 — 自研微内核替换（M1~M4 稳定后独立排期）
- 范围：固定优先级抢占 + PendSV/SysTick 1ms + 信号量/互斥/延时 + 栈金丝雀（~400 行，静态数组，不做时间片/计数信号量/动态内存）
- 因 D2 抽象层存在，替换 = 新内核实现 `rtos_api` + M1 验收用例回归
- 验收：M4 全部用例在新内核上重跑通过；若排期/稳定性受限，降级为"自研内核独立玩具工程"，两种形态均可写简历

## 3. 风险与回退

| 风险 | 对策 |
|---|---|
| SCSI 任务化改动 ST 库过深 | M3-A 先行，B 移植可回退 A，不影响其他里程碑 |
| 自研内核不稳定 | 抽象层保证随时切回 FreeRTOS，业务代码零改动 |
| RAM 紧张（20KB） | FatFs 裁剪、单扇区缓存、环形缓冲 4~8KB 内、关 LFN |
| ——（SPI1 采用默认映射，无 NOJTAG 改动，本风险不适用） | — |

## 4. 简历话术映射（实现后落笔）

- USB MSC+HID 复合设备 + SCSI 任务化 → "中断内不做阻塞 IO，长操作任务化"
- FatFs + 环形缓冲日志 → "日志不丢帧、掉电安全"
- 单写者闸门 → "主机占用与本地写入的互斥设计"
- rtos_api 抽象 + 自研内核（M5） → "调度器原理 + 可替换架构"
- 功耗/24h 实测数字 → "可量化验收"

## 5. 打开项（执行前确认）

- [x] PB12（SD CS）空闲；PA8 不再需要（nRF IRQ 保持 PB5）
- [x] SD 模块：VCC=5V、SPI 六线、3.3/5V 电平兼容、FAT16/FAT32（模块用户文档确认）
- [x] 调试器 SWD（ST-Link）
- [x] UART1 保留（printf 调试）
- [x] 日志格式：二进制定长块 + Python 解析（默认）
- [ ] 实物接线最终核对（含 PA8/PB12 空脚、模块 5V 供电）
- [ ] SD 卡准备：≤32G、格式化为 FAT32 后实测

## 6. 执行期新增记录

- 2026-09-05：参考例程甄别——FATFS/USB-Mass-SD 例程介质层为 SDIO（C8T6 无 SDIO，仅概念参考）；SPI-SD 逻辑蓝本采用 `参考历程/SD/utility/Sd2Card.cpp`；FatFs 用新版 R0.15（见 1.1 索引）
- 2026-09-06：M3 硬件验证通过——USB HID+MSC 复合设备功能正常（鼠标正常、U盘识别、文件拷贝正确）

## 7. 后续优化方向（M4 及以后可选）

### 7.1 SD 传输性能优化（当前瓶颈：~4MB/2-3min）

| 优化项 | 预期收益 | 复杂度 | 说明 |
|---|---|---|---|
| DMA1_CH2/CH3 替代阻塞式 SPI | 写速率 2~5× | 中 | CubeMX 已配置 DMA 句柄，需改 `sd_xfer()` 为 DMA 中断驱动，释放 CPU 做其他任务 |
| CMD18/CMD25 多块读写 | 写速率 3~10× | 中 | 当前 CMD17/CMD24 单块，每块需命令+地址+CRC 开销；多块连续传输减少命令开销，配合 pre-erase 可进一步提速 |
| SD 预擦除（CMD33/34） | 写速率额外 2~5× | 低 | 写前一次性擦除目标范围，消除 SD 内部逐块擦除等待（写后 wait_ready 1-10ms/块 → 接近 0） |
| SPI 时钟提速 | 线性提升 | 低 | 当前 18MHz，部分 SD 卡 SPI 模式支持 25MHz+；需验证信号完整性 |
| 写缓存（RAM 缓冲合并） | 写延迟降低 | 低 | 多小块合并为单次大块写，减少 SD 编程次数 |

### 7.2 USB 枚举速度优化

| 优化项 | 预期收益 | 说明 |
|---|---|---|
| SD preinit 并行化 | 枚举快 ~0.5-1s | SD 初始化与 USB 描述符准备并行（当前串行：SD preinit → USB start） |
| INQUIRY 缓存 | 减少首次延迟 | 开机预缓存 INQUIRY 响应，避免枚举时查 SD |
| 快速 SD 检测 | 减少枚举抖动 | 用 CMD0 软检测替代完整初始化序列判断 SD 是否就位 |

### 7.3 可靠性（M4 原始范围）

| 优化项 | 说明 |
|---|---|
| IWDG 看门狗 | 空闲任务喂狗，防止任务死锁/SD 卡死导致系统挂起 |
| 栈高水位监控 | FreeRTOS `uxTaskGetStackHighWaterMark` 定期检查，UART 报告 |
| 24h 连续运行测试 | 日志完整性 + U盘拷贝校验 + 鼠标响应 + 无复位 |
| 功耗量化 | 空闲/工作电流表测量，识别优化空间 |
| 丢包/误码统计 | nRF 收包 CRC 校验失败计数，UART 报告 |

### 7.4 功能扩展（可选）

| 方向 | 说明 |
|---|---|
| SD 卡热插拔检测 | CS 引脚检测或 SD DETECT 引脚，支持运行时换卡 |
| 日志文件轮转 | DATA.LOG 达到阈值后自动创建 DATA001.LOG 等新文件 |
| OLED 状态显示 | 显示 USB 连接状态、SD 卡容量、日志写入速率 |
| TX 算法优化 | 利用日志数据离线标定分析（D7 解析脚本已就绪） |
| HID 扩展功能 | 键盘/媒体控制复合（需修改报告描述符） |
