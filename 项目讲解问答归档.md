# 项目讲解问答归档（教学记录）

> 用途：把"项目讲解"过程中的**用户提问与我的回答**逐一归档，便于回看与追溯。
> 维护：每次讲解/答疑后在此追加条目（时间｜第几讲｜问题｜回答要点）。

---

## 2026-09-06 第1讲《系统总览与启动流程》

- 用户提问：无（本讲为开篇）。
- 回答要点（归档）：
  - 系统=2.4G空中鼠标的 RX：TX(MPU6050+nRF24L01) → 2.4G → RX(nRF24L01, SPI2) → USB MSC+HID 复合（鼠标+HID 上报 / U盘=MSC）＋ SD 日志(FatFs)。
  - 引脚：SPI2 PB13-15 + CE PB0/CSN PB1/IRQ PB5；SPI1(PA5-7) 接 SD、CS PB12；USB PA11/12；UART1(PA9/10) 调试；OLED 软I2C PB10/11。
  - 上电顺序（main.c）：HAL_Init→时钟(72MHz/USB48)→GPIO→DMA→USART1→TIM3(1ms 运行统计源)→SPI2→SPI1 → USER2：NRF_Demo_Init(OLED+NRF自检+RX_Mode) → sysmon_init_hw(IWDG 有界等待) → usb_storage_preinit(SD初始化+容量缓存) → usb_storage_msc_task_init(SCSI信号队列) → **MX_USB_DEVICE_Init**(注册复合类+storage后启动USB) → app_start(建任务→vTaskStartScheduler)。
  - 任务：rf_rx(160w/5, 10ms收包→HID+日志)、scsi_msc(256w/4, 延迟执行SD读写续跑BOT)、sd_log(220w/3, 队列→DATA.LOG 批量写)、sysmon(300w/1, CPU/栈/链路+喂狗)、IDLE(喂狗)。heap 10240B；业务经 rtos_api 访问内核。
- 待用户确认理解后进入第2讲。

## 2026-09-06 第2讲《nRF24L01 模块》大纲（待用户确认第1讲后开讲）
- 待讲范围：
  - 驱动层：SPI2 接线与 NRF24L01.h 引脚宏；命令/寄存器读写原语(sd 风格)；初始化(TX/RX_Mode 寄存器配置 2Mbps/CRC/地址/频道/自动应答重发)；收发函数与超时保护；与"正点原子"原驱动的差异点
  - 应用层：NRF_Demo.c 的角色与初始化(NRF_Demo_Init 自检重试/RX_Mode)、rf_rx 任务收包→MousePacket_t 32B 结构→HID/日志
  - Bug 与修法：冷启动 NRF ERR(延时+重试3次)；事件驱动丢包率统计口径；收发包阻塞与超时

## 2026-09-06 第1讲 补充答疑（用户 3 问）
- Q1：为什么 USB 启动前 SD 必须就绪，否则 U盘 会失败？
  A：Windows 枚举后约 1~2s 内 usbstor 就会发 SCSI INQUIRY/READ CAPACITY/READ(10) 读扇区。SD 初始化(CMD0..ACMD41+CSD)需几十 ms~1s 且可能重试；若主机来问容量/读扇区时 SD 尚未就绪，介质层返回失败或 0 容量 → Windows 判定"无法启动(0xA)"或不出盘符并反复重试。因此把 SD 初始化+容量缓存放在 MX_USB_DEVICE_Init(拉 D+)之前，并让 storage 每次访问还能"确保 SD 就绪"兜底。
- Q2：SCSI 是什么？"收到 SCSI 读写请求后在任务里执行 SD 读写再续跑 BOT"不懂。
  A：USB MSC 分两层：BOT=传输层(CBW/CSW 在 bulk 端点上的信封格式)；SCSI=命令集(INQUIRY/READ CAPACITY/READ10/WRITE10 等业务命令)。主机读写文件扇区=发 READ10/WRITE10。朴素做法是 USB 中断里直接读 SD(SPI, ms 级阻塞)再回数据——中断被占 ms，主机超时/降速/出错。任务化：中断只做"登记请求+ISR 安全信号量唤醒 scsi_msc 任务"立即返回；任务空闲时执行 SD 读/写，再回调 USB 栈函数(USBD_LL_Transmit/PrepareReceive/发 CSW)让 BOT 状态机继续。详细(信封结构/影子缓冲/状态机)留到 USB 模块细讲。
- Q3：上电顺序还有什么讲究？
  A：原则=“慢/阻塞初始化全部放在 USB Start(拉 D+)之前；之后只留轻量动作与调度器任务”。要点：①先时钟(USB 需 48MHz)再初始化；②HAL tick 单源后才用延时；③SPI1/SPI2/TIM3(1ms 运行统计)先于各自使用方；④NRF/OLED 先做(与 SD/USB 无争用、可早显示错误)；⑤SD 预初始化+SCSI 队列必须在 USB 启动前；⑥IWDG 用有界等待启动，绝不阻塞引导；⑦MX_USB_DEVICE_Init 被从 CubeMX 自动段移到 USER2 正是为了控制相对 SD 的顺序。

## 2026-09-06 第1讲 补充答疑（用户 4 问）
- Q1：串口 DATA.LOG open tick≈4000+ 是 SD 初始化 4s？
  A：不是。sd_log.c 开头上电**故意延时 3s**（rtos_delay(3000)，错峰避免开机即写卡电流尖峰/USB 枚举失败）；open tick≈4.3s ≈ 启动/挂载耗时(~1s) + 3s 延时。SD 单次初始化本身几十~几百 ms（偶发失败会重试更长），且真正的 SD 初始化+容量缓存在更早的 usb_storage_preinit() 已完成（tick 很小）。
- Q2：U盘 是批量传输，为什么是"USB 中断"？
  A：两种"类型"不同维度的东西。批量/中断/同步是**端点传输类型**（决定主机怎么调度、每事务大小/间隔；HID 用中断 EP、MSC 用批量 EP）。但设备端 CPU 并不知道"数据到了/传完"，F103 USB FS 没有 DMA，**每个端点事件都要触发 USB 中断**（USB_LP_CAN1_RX0_IRQHandler→HAL_PCD_IRQHandler）才能让固件响应。所以"在 USB 中断里做 SD 读写"指的就是在这个 ISR 上下文里处理 bulk 事件——不是批量传输就不需要中断。
- Q3：HAL tick 的 1ms 是 TIM3 提供的？
  A：不是。HAL tick 由 **SysTick** 单源提供（SysTick_Handler 只调一次 HAL_IncTick；FreeRTOS tick 也复用同一 SysTick，调度器启动后再调 xPortSysTickHandler）。**TIM3 的角色是"FreeRTOS 运行时间统计"的计数源**：1ms 溢出 ISR 递增 g_ulRunTimeCounter，供 vTaskGetRunTimeStats 算 CPU%；启动由 tim.c 的 vConfigureTimerForRunTimeStats 在调度器初始化时调用。两者都≈1ms，但职责不同。
- Q4：SCSI/BOT 细节留到 USB 模块。
  A：收到，已安排（信封结构、影子缓冲、状态机续跑等会在 USB 部分细讲）。

## 2026-09-06 第1讲 补充答疑 3（用户 2 问）
- Q1：USB 模型理解确认——主机对不同端点发命令，STM32 进 USB 中断按"命令值+端点号"分发到服务函数（现场执行或给 FreeRTOS 任务发信号量），对吗？
  A：方向正确，补充两个细节：①"命令"分两类——EP0 控制传输(枚举/类请求，按 bRequest+wIndex 分发到 core/class Setup，多在中断内直接应答) 和 端点数据传输(EP0 之外：MSC 的 CBW/CSW/数据走批量端点 → class DataOut/DataIn 按端点号分发；HID 的上报是任务主动发而不是主机命令)。②"释放信号量/唤醒任务"只用于耗时路径(READ10/WRITE10 的 SD 读写，经 ISR 安全信号量唤醒 scsi_msc 任务后由任务回调续跑 BOT)；EP0 类请求与 CSW 等轻量步骤在中断内直接完成。另注意方向不对称：USB 既有主机→设备的命令，也有设备主动发起(如 HID IN 报告)。
- Q2：教程说把 SysTick 给 FreeRTOS、HAL 用别的定时器；HAL 与 FreeRTOS 共用 SysTick 会冲突吗？
  A：可以共用，本项目即共用且验证正常：同一 SysTick ISR 里先 HAL_IncTick()(HAL tick)，调度器启动后再 xPortSysTickHandler()(FreeRTOS tick)，两者都 1kHz。可共用的前提：两边 tick 周期一致(HAL 1ms 与 configTICK_RATE_HZ=1000)、SysTick 优先级满足 FreeRTOS(内核最低)、调度器未启动时不要调 xPortSysTickHandler(代码有守卫)。教程里"HAL 另用定时器"多见于需要不同 tick 频率/功耗(tickless)或移植历史，并非必须。共用需注意的坑：改 configTICK_RATE_HZ 会同时改变 HAL 延时标尺；别在 SysTick ISR 里做长活。

## 2026-09-06 第1讲 补充答疑 4（名词词汇表：USB 分发链路相关名字）
- 用户：bRequest/wIndex/comp_setup/comp_data_in… 这些名字不知道指什么
- 回答要点：
  1. USB 控制传输"Setup 包"8 字节字段：bmRequestType/ bRequest(请求号,如 6=GET_DESCRIPTOR, 0xFE=GET_MAX_LUN)/ wValue(主参数:描述符类型等)/ wIndex(通常接口号或端点号)/ wLength(期望长度)。解析后存进 USBD_SetupReqTypedef。
  2. Core = ST USB 库中间层(usbd_core/ctlreq)：统一处理标准请求并回调"类"。我们注册的类是 USBD_Composite(usbd_composite.c)，其成员就是函数指针：Init/DeInit/Setup/DataIn/DataOut/几个 GetXxxConfigDescriptor。
  3. 我们在 usbd_composite.c 里写的静态函数 comp_init/comp_setup/comp_data_in/comp_data_out 被赋给上面那些成员 → Core 调 pdev->pClass->Setup(...) 实际进入我们的 comp_setup。
  4. comp_setup 收的是 EP0 控制请求：switch(wIndex/bRequest) 路由到 HID 的 USBD_HID_Setup 或 MSC 的 USBD_MSC_Setup（ST 类源码自带同名函数）。
  5. comp_data_in/out 由 Core 在"某端点 IN 传输完成 / OUT 收到数据"时回调，参数 epnum(端点号)；按端点号路由：1→HID、2→MSC。
  6. 之前串口 [CMP] setup t=.. r=.. i=.. 里的 t/r/i 分别对应 bmRequestType/bRequest/wIndex。
  7. 详细协议(CBW/CSW 内部字段等)留 USB 模块讲解；本讲只到"能读懂这些函数名与调用链"。

## 2026-09-06 第1讲 补充答疑 5（comp_data_in/out 如何知道端点号）
- 用户：comp_data_in/out 怎么知道"目标端点号"来路由？
- 回答要点：
  1. 端点地址是我们自己定死的宏：HID 用 IN 0x81；MSC 用 IN 0x82 / OUT 0x02（配置描述符里声明、Init 里 OpenEP 都用同一宏）。
  2. 硬件事件到达：F103 USB 中断(HAL_PCD_IRQHandler)解析"哪个端点+方向"后回调 usbd_conf 里的 PCD 回调(DataInStage/DataOutStage…)，回调里带着该端点的地址。
  3. Core(usbd_core)收到后调用 pdev->pClass->DataIn/DataOut(pdev, epnum)——epnum 就是硬件带过来的端点地址。
  4. 我们的 comp_data_in/out 只要把 epnum 的方向位去掉(epnum & 0x7F)再和宏比较即可路由：1→HID、2→MSC。
  5. 代码即上：switch(epnum & 0x7F) case (HID_EPIN_ADDR&0x7F)… case (MSC_EPIN_ADDR&0x7F)…

## 2026-09-06 教学风格调整 + 第1讲通过 + 第2讲(源码导向)
- 用户指示：后续讲解积极加入源码讲解；可以继续下一讲。第1讲（总览/启动/任务/USB分发词汇）视为通过。
- 风格：每讲先给"模块地图→驱动层(寄存器/时序/源码)→应用层(接入/任务/源码)→踩坑"。
- 第2讲《nRF24L01 模块》已讲解（源码导向），待用户确认后进入第3讲《SD 卡(SPI)驱动》。

## 2026-09-06 第3讲《SD 卡(SPI1)驱动》大纲（待用户确认第2讲后开讲）
- 驱动层：SPI1(PA5-7)+CS PB12；sd_spi 直接寄存器全双工原语；CMD0/CMD8/ACMD41/CMD58(OCR) 初始化流程与 SDHC 判定；单块 CMD17/24、多块 CMD18/25(默认关)；CSD 容量解析(SD_GetBlockCount)；busy/ready 与超时
- 应用层：usb_storage_preinit(开机预初始化+容量缓存)；diskio(卷0=SD) 给 FatFs；sd_log 任务 3s 错峰+队列+DATA.LOG；SD_USE_MULTI 实验结论
- 踩坑：结构体对齐 16B(pack)；"SD 初始化 4 秒"误解(3s 是延时)；EP/PMA 与 SD 无关的 USB 侧问题排除；SPI 分频漂移

## 2026-09-06 第2讲 nRF 答疑 1（用户 6 问）
- Q1 HAL_SPI_TransmitReceive 后两个参数：Size(数据项数，8位=字节) 与 Timeout(ms)。
- Q2 Write_Reg 的 status：SPI 全双工，发命令字节时同步读回芯片 STATUS；真实代码有 `status = SPI_ReadWriteByte(reg);`（上一讲漏抄，已纠正）。
- Q3 "写命令"：事务首字节=命令(0x20|reg 写寄存器 / 0xA0 W_TX_PAYLOAD / 0x61 R_RX_PAYLOAD 等)，CSN 低-高 作帧边界。
- Q4 buf 读写=定长 for 循环逐个字节收发，len 由常量决定(5/32)。
- Q5 Init=只配 CE/CSN/IRQ 的 GPIO(+开时钟)，射频参数在 TX/RX_Mode。
- Q6 Check=写 5×0xA5 到 TX_ADDR 再读回比对(基于 Write/Read_Buf)，探测芯片存在与 SPI 通断。
- Q7 TX_Mode 各寄存器逐位：地址/P0地址/EN_AA/EN_RXADDR/SETUP_RETR(500+86us×10)/RF_CH=40(2440MHz)/RF_SETUP(2Mbps/0dBm)/CONFIG(CRC16+PWR_UP; PRIM_RX 0=TX,1=RX)。

## 2026-09-06 第2讲 nRF 答疑 2（用户 2 问）
- Q1：NRF24L01_Check() 若 SPI 异常返回什么？
  A：只返回两态 0(在)/1(不在)，不返回 SPI 错误码。SPI 异常三路径都收敛到 return 1：①HAL_SPI_TransmitReceive 超时(100ms)时 rx 保留初值 0xFF；②MISO 悬空/断线读回 0xFF/0x00；③字节错位(0xA5=1010 0101 黑白相间测试图案，错一位立刻暴露)。诊断 SPI 本身需示波器看波形或读 STATUS 固定位，Check 只适合开机自检 [BOOT] nrf-ok/fail。
- Q2：RX_ADDR_P0 为什么带 P0？
  A：P0=Pipe0 数据管道 0，是寄存器名一部分。nRF 有 6 个接收管道(P0~P5, 地址寄存器 0x0A~0x0F)，P0/P1 是 5 字节地址，P2~P5 仅 1 字节 LSB；TX_ADDR(0x10) 是发送地址寄存器，不属于任何管道。TX_Mode 里也写 RX_ADDR_P0 的原因：ShockBurst 自动应答的 ACK 包目标是"本机"，硬件规定发送方必须用 P0 地址匹配该 ACK，RX_ADDR_P0 必须等于 TX_ADDR，否则收不到 ACK、TX_DS 永不置位→重发到 MAX_RT。本项目两端地址同为 {0x34,0x43,0x10,0x10,0x01}（NRF24L01.c:21-22, 222-223）。

## 2026-09-06 第2讲 nRF 答疑 3（用户 2 问：SPI 全双工与 rx=0xFF）
- Q1：HAL_SPI_TransmitReceive(&hspi2,&byte,&rx,1,100) 是干什么的？
  A：SPI 是同步全双工总线(SCLK/MOSI/MISO/CSN)，无独立读写：每个时钟沿主机从 MOSI 移出一位同时从 MISO 移入一位，所以"读从机"也必须先发一个字节产生时钟。HAL_SPI_TransmitReceive = 同时收发一个字节(Size 数据项数, Timeout 100ms 限时)，byte 发出去、rx 收回 MISO 上的数据。封装 NRF24L01_SPI_ReadWriteByte 就是"发一个收一个"。Read_Reg 时序=CSN低→发寄存器号(同时收回 STATUS,丢弃)→发 0xFF 作 dummy 产生时钟把寄存器内容移回→CSN高；Write_Reg 则保留第一个字节收回的 STATUS 返回给调用者。
- Q2：rx 一直初始化为 0xFF 是干什么？
  A：两作用：①错误兜底——HAL 调用超时/失败时不写 rx，无初值则返回栈垃圾值；初始 0xFF 保证任何失败路径返回确定值(与 Check 判 0xA5 失败路径自洽)。②惯例——0xFF 全 1 是 SPI 读操作标准 dummy 字节(MISO 从机不驱动时上拉读回 0xFF；SD/nRF 要求 MOSI 空闲高)，Read_Reg 第二句就是显式用 0xFF 做 dummy。真实读数据是 HAL 成功时从 MISO 移入 rx 的，与初值无关。

## 2026-09-06 第2讲 nRF 答疑 4（用户问：CSN低→发寄存器号 是否让nRF下一次通信对该寄存器读写）
- A：纠正关键点——不是"下一次通信"，命令字节本身就是当次事务的完整指令，读写当场完成，nRF 不是 I2C EEPROM 那种跨事务记忆模型。命令字节结构：bit7:6=00(寄存器命令族)、bit5=读写方向(0=读 R_REGISTER 基址0x00 / 1=写 W_REGISTER 基址0x20)、bit4:0=寄存器地址(5位)。所以代码里写寄存器传 NRF_WRITE_REG+reg(0x20|reg，如 0x2A=写RX_ADDR_P0)，读时直接传 reg(0x00|reg，如 0x10=读TX_ADDR)。事务=CSN低→[命令字节+数据/dummy]→CSN高，写入在CSN拉高瞬间锁存，读取在dummy时钟沿从MISO移出，均在同一事务内完成。另有特殊命令族不编码寄存器地址(0xA0 W_TX_PAYLOAD、0x61 RD_RX_PLOAD、0xE1/0xE2 FLUSH_TX/RX)，操作对象是收发FIFO。

## 2026-09-06 第3讲 SD 答疑 1（用户 14 问：寄存器vs HAL/阻塞/DR双缓冲/速率/时钟/inline/arg/指令集/容量等级/OCR/返回值/CSD/diskio）
- Q1 直接写寄存器 vs HAL 区别："逐字节"辨析：8 位 SPI DR 一次 1 字节，两者都逐字节；HAL 每字节多一圈状态机/锁/超时检查(5~10μs/字节注释量级)，寄存器直写 4 步几百 ns。nRF 32B 低频用 HAL 无所谓，SD 512B 高频换寄存器。
- Q2 阻塞/非阻塞：阻塞=轮询到完成才返回(无后缀 API、内部 while 等标志)；非阻塞=发起即返回(_IT/_DMA，完成靠回调)。判别看命名/回调。长阻塞放任务 OK，绝不能放中断——MSC 的 SD 操作任务化正为此。
- Q3 DR 写覆盖读？不会——STM32 SPI DR 读写是两个物理缓冲(写=Tx缓冲，读=Rx缓冲)，靠访问方向区分。时序：等TXE→写DR(进Tx,移位输出,同时MISO采样移入)→8时钟后RXNE=1→读DR(取Rx)。写前等TXE、读前等RXNE防挤掉/overrun。F1 SPI 无硬件 FIFO。
- Q4 sd_spi_speed 调用次数：非一次。每次 SD_Init 都低速(187行)→高速(288行)各一次；SD_Init 被多处多次调用(preinit/每次f_mount的disk_initialize/MSC ensure兜底)。MODIFY_REG=先清BR位再写prescaler；枚举值本身就在BR位域位置。
- Q5 sd_dummy_clocks 为何不用 HAL_Delay：SD 卡状态机由 SCLK 沿驱动，要的是时钟脉冲不是时间；HAL_Delay 不产生 SCLK。0xFF=MOSI 空闲高+每字节 8 个时钟沿。
- Q6 inline：建议编译器调用处展开，省压栈/跳转；适合小函数高频(512 次循环)。只是建议，低优化等级可能不展开。
- Q7 cmd 前导 0xFF 时钟：CSN 刚拉低给卡稳定缓冲 + 冲刷上一操作(MISO 残留)的时钟间隙。
- Q8 arg 规则：每条命令自定含义(CMD0/9/55/58=0；CMD8=0x1AA 电压检查；ACMD41 bit30=HCS；CMD17/24=地址[SDSC字节/SDHC块])，发送大端(高字节先)。
- Q9 命令集：SD 有 CMD0~63 + ACMD，权威=SD Physical Layer Simplified Specification(官网 PDF)第 7 章命令表，或 SdFat 头文件。命令帧=0x40|idx+4B arg+CRC；SPI 模式仅 CMD0/CMD8 CRC 强制，其余可 dummy(0x01)。项目用 CMD0/8/9/17/24/55/58+ACMD41。
- Q10 SDSC(≤2GB,字节寻址,CCS=0)/SDHC(2-32GB,FAT32,块寻址,CCS=1)/SDXC(32GB-2TB,exFAT,块寻址,CCS=1)；s_hc=CCS 决定 block<<9 与否。
- Q11 OCR 后 3 字节：CMD58 响应=R1+OCR(4B)；只用到第一字节(bit31 上电完成/bit30 CCS)，余 3 字节必须用时钟完整移出(SPI 无"不要了"机制，不读完污染总线)。
- Q12 fast_read/write 是阻塞轮询(逐字节等标志)。返回值的"含义"无法预知，靠协议时序位置+特征采样解读：命令后 (r&0x80)==0 认 R1、0xFE 认数据令牌、(b&0x1F)==0x05 认写应答、0xFF 认空闲/不忙。
- Q13 CSD=卡特定数据寄存器 128 位(CMD9 读出 16B)。v2: 块数=(C_SIZE+1)<<10(C_SIZE 单位 512KB)。v1: 块数=(C_SIZE+1)×2^(C_SIZE_MULT+READ_BL_LEN−7)。位→字节映射 byte=(127−bit)>>3；给出 C_SIZE/C_SIZE_MULT/READ_BL_LEN 落到 csd[5..10] 各掩码的验证。
- Q14 diskio.c 里 FatFs 引用=include ff.h/diskio.h + 函数签名全用 FatFs 类型(DSTATUS/DRESULT/LBA_t/STA_NOINIT/RES_OK)；它是被 FatFs 内核调用的底层插槽，不调上层。真正用 f_mount/f_open/f_write/f_sync 的是 sd_log.c。

## 2026-09-06 第4讲 FatFs 答疑 1（用户 12 问：FATFS字段/簇/MBR/DBR/FAT2镜像/术语/卷/挂载/目录项/两段recv/win窗口）
- Q1 FATFS 逐字段：fs_type(0未挂载/FAT12/16/32) pdrv(物理盘,本工程0) ldrv(逻辑盘,仅REENTRANT) n_fats(1或2) wflag(win脏b0) fsi_flag(FSInfo状态) id(挂载ID,检测对象失效) n_rootdir(FAT12/16固定根目录项数,FAT32=0) csize(每簇扇区数) n_fatent(FAT项数=簇数+2) fsize(每FAT扇区数) winsect(win缓存的是几号扇区) volbase(卷起始) fatbase(FAT1起始) dirbase(FAT12/16=扇区/FAT32=根簇) database(数据区起始=簇2) win[512](FAT/目录扇区缓存)。条件编译:本工程 512=512 无ssize, LFN=0 无lfnbuf, RPATH=0 无cdir, EXFAT=0 无exfat块, READONLY=0 有last_clst/free_clst。
- Q2 扇区=512B 介质传输单位; 簇=csize扇区 文件系统分配单位; FAT一项=一簇状态。
- Q3 MBR 非页表: 整盘唯一在第0扇区(446B引导+64B分区表+55AA), 只管"盘→分区", 挂载时查一次填volbase后不再碰, 非每次访问查。
- Q4 DBR 每分区首扇含 BPB: 每扇区字节/每簇扇区(csize)/保留扇区/FAT份数(n_fats)/每FAT扇区(fsize)/根簇(FAT32)/总扇区(推簇数→n_fatent)。修正: DBR不直接存簇总数, 由总扇区-csize 推出。
- Q5 FAT2 镜像只镜像分配表非数据; 容量计算已刨除FAT区; 1GB卡两份FAT≈240KB(0.02%级), 非减半; 开销在卡上非RAM; 目的=容错(chkdsk用FAT2修FAT1)。
- Q6 术语表: MBR/DBR/VBR/BPB/FAT/FSInfo/FIL/FATFS/LFN/FR_xxx/LBA/csize/fsize/n_fatent 全称含义(见正文)。
- Q7 卷=可被文件系统识别的存储单元(物理盘可1卷或多分区多卷); FF_VOLUMES=1→卷0; 路径"0:"前缀。
- Q8 挂载=把盘上文件系统元数据读出登记成可用卷; f_mount(,1)=立即挂载读MBR/DBR/BPB填FATFS; 纯软件不写盘。
- Q9 32B目录项=每文件/子目录独有1项(8+3名+属性+起始簇高16+低16+大小4B); 删除=0xE5, 0x00=目录尾。f_open 只扫目标目录的簇链(几百项), 非全卡遍历, 命中即停。
- Q10 两段recv互补: ⑤while非阻塞=一次清空积压攒批; ⑦500ms阻塞=队列空时让出CPU且有界睡眠; 若⑦无限阻塞则⑥永不执行→1s超时flush永不触发; 500ms保证每500ms回⑥检查。有消息则append+continue不等满超时。
- Q11 win[]≠写数据缓冲: 文件数据经应用buffer直写disk_write不碰win; win缓存FAT/目录元数据扇区(winsect=当前扇区号,wflag=脏), 命中同扇区直接内存改, 换出/fsync时脏才写回=write-back策略。

## 2026-09-06 第4讲 FatFs 答疑 2（用户 6 问：n_fatent+2/多级目录/recv循环/MBR盘符/挂载确认/win写回）
- Q1 n_fatent=簇数+2: FAT表项号=簇号, 但簇0,1保留(项0=介质描述符,项1=脏/IO错误标志), 真实簇从2号起(database=簇2), 表项数=0+1+簇数+2→簇数+2。free_clst 判无效用 n_fatent-2。
- Q2 多级子目录=沿路径逐层下探(follow_path): 每层只在当前目录簇链线性比对, 命中子目录项(属性0x10)取其记录的起始簇作下探指针, 到下一层目录簇链继续。目录项即树节点, 无全局标志, 非全盘搜。
- Q3 任务循环: ⑤是while非阻塞, 积压N条一次全收(每收一条立刻再recv直到空), qbuf仅单条暂存; ⑥每次循环都执行的前提是循环转得回⑥, ⑦必须带500ms超时而非无限阻塞: 无限阻塞时无消息则永远睡在⑦, 1s超时flush与usb_busy检查永不执行; 500ms有界睡眠保证节拍, 有消息则append+continue不等超时。
- Q4 MBR分区表在磁盘层切分物理盘, 告知对象是主机OS非芯片; 每个含可识别文件系统的分区=1卷=可1盘符; 单分区整卡1盘符; 注意Windows对可移动介质默认只给第一个分区分配盘符(固定盘才全部分区)。FatFs挂载同Windows: 读扇区0若MBR找第一个FAT分区, 无MBR(整卷DBR)也能挂。
- Q5 挂载=文件系统层确认(读MBR/DBR/BPB验签名载入FATFS), 区别于介质层disk_initialize(SD_Init握手确认卡硬件); 顺序 disk_init→mount→open→IO; mount fail fr=3 根因多为disk_initialize失败。
- Q6 win窗口理解正确=write-back(写回)缓存: 缓存当前FAT/目录扇区, winsect记扇区号, wflag记脏; 换扇区时若脏先写回winsect, 再读新扇区; f_sync=强制刷脏。

## 2026-09-06 第4讲 FatFs 答疑 3（用户 2 问：FatFs格式化是什么/MBR是否必要）
- Q1 FatFs格式化：格式化=在介质上从头建文件系统结构。术语澄清: FatFs 格式化能力=可选 f_mkfs()(FF_USE_MKFS 控制, 本工程=0 关闭→固件不能自格式化, 只能挂载已存在 FS)。FAT32 格式化流程: 写MBR+分区表(分区方式) → 写DBR+BPB → 初始化FAT1/2(项填0空闲, 项0/1保留) → 写FSInfo → 建空根目录(0x00结束项); 快速格式化只重建结构不清数据。效果=摧毁旧目录/FAT。本卡 DATA.LOG 可写说明卡上已有 FAT32(PC格式化过); 空白无FS卡→f_mount失败=mount fail 刷屏根因。
- Q2 MBR非必需: FatFs 卷两种形态——带MBR(扇区0=MBR→找第一个FAT分区跳进去读DBR, Windows U盘默认) 与 无MBR superfloppy(扇区0直接=DBR→整卡一卷, 软盘/相机卡常见)。f_mount 先试探扇区0是否MBR, 两种都支持。MBR是跨系统通用性惯例(BIOS/OS磁盘工具按分区表路径找卷)非FS层需求。三层关系: 分区(MBR切块)→格式化(DBR/BPB定义FS)→文件目录(FAT+目录项存文件)。本卡=MBR单FAT32分区, Windows与FatFs行为一致。

## 2026-09-06 第4讲 FatFs 答疑 4（用户 2 问：卡何时格式化/插新卡是否识别失败）
- Q1 格式化何时完成：非本设备——固件全工程无 f_mkfs/MKFS=0, 只挂载+追加写; 卡在进入系统前已格式化, 两种可能: ①≤32GB SD 出厂预格式化 FAT32(1GB 卡最可能) ②早期 PC 读卡器格式化(模块文档要求"使用前格式化 FAT16/FAT32", 开发日志:222)。验证: 读卡器插 PC 看盘符属性 FAT32。
- Q2 插新卡是否失败：分两条链路。A) MSC/SCSI 设备层(枚举/容量/扇区读写)对新卡免疫——容量来自 CSD 与 FS 无关, [CAP] preinit 照常, Windows 能枚举出大容量存储设备与磁盘; 之前的"无法识别/事件411"是供电/枚举/协议层问题, 换卡不重现。B) 盘符/文件可见性取决于卡上 FS: 出厂 FAT32(≤32GB)→盘符✅日志✅(f_open 自动建 DATA.LOG); 出厂 exFAT(≥64GB)→Windows 盘符✅(usbstor 与 FS 无关, 自带 exFAT 驱动) 但固件 FF_FS_EXFAT=0→mount fail 日志不写; 完全空白→Windows 提示"需要格式化"/未分配盘符, 固件 mount fail。关键: 空白卡不会导致 USB 无法识别(那是硬件问题), 只会没盘符+日志失败; 固件 MKFS=0 无法自格式化→需 PC 格式化 FAT32, 或后续增强打开 MKFS+f_mkfs 实现插卡即用。

## 2026-09-06 第5讲 USB 答疑 1（用户 15 问：复合描述符通用性/句柄/fops/分片传输/影子拷贝/句柄切换/PMA细节/CSD/INQ重发/无盘符/1-2分钟）
- Q1 复合 vs 单设备描述符：结构同=标准+接口类描述符+端点描述符；复合=bNumInterfaces=2+两套接口/端点块；任意复合扩展同流程。枚举完成=主机"认得出是 HID+MSC 复合"(设备管理器两节点)，但盘符要等 SCSI 探测完。
- Q2 s_hid_h/s_msc_h=void* 实例句柄(存 USBD_HID.Init/MSC.Init 时 pdev->pClassData)，comp_* 用 set_child 切换 pClassData 调子类回调。BYTE=字节(uint8 的 Windows typedef)。
- Q3 READ10/WRITE10 进行中任务切换/HID 不干扰：端点硬件队列+中断逐包推进与 CPU 任务无关；HID 走 EP1 IN、MSC 走 EP2 互不干扰；真正共享资源只有 SD 总线(usb_busy 门控管 sd_log)。
- Q4 Windows 操作 U 盘内容全化为 READ10/WRITE10(LBA 扇区)，MSC 层无文件/簇概念；每次最小单位=扇区512B, 可多连续扇区(scsi_blk_len)。
- Q5 (void)param;=消除未使用形参告警。
- Q6 usbd_msc_get_fops()=ST 全局访问器返回 USBD_RegisterStorage 注册的 fops 表(即 usb_storage.c:178 USBD_SD_Storage_fops)，->Read 即 sd_storage_read→SD_ReadBlock。
- Q7 else 分支一次最多 512B(MSC_MEDIA_PACKET=512)：读→Transmit→scsi_blk_addr/len 递减→剩余则下次 DataIn 中断再 ProcessRead 唤醒→直到 len==0 置 LAST_DATA_IN→SendCSW(PASSED)。大文件=512B×N 分片流水。
- Q8 WRITE 影子拷贝位置：SCSI_ProcessWrite(usbd_msc_scsi.c:740) memcpy(s_write_shadow, hmsc->bot_data, len)——bot_data[512] 是复用收包缓冲必须先取走；任务从 shadow 写 SD 再 PrepareReceive 布下一包。READ 反向：任务读进 bot_data→Transmit。
- Q9 句柄共用：全局唯一 hUsbDeviceFS，切换的是 pClassData 类实例指针(非句柄)；HID SendReport 内部靠 pdev->pClassData 找 HID 实例→comp 回调必须恢复 s_hid_h。
- Q10 PMA 细节(开发日志:101-110)：F103 无独立 RAM，每活跃端点须在 PMA 手工分配缓冲，OUT 无 PMA=收不到数据；当时只配 3 条缺 MSC OUT→只收第一条 CBW 后续命令永远收不到→超时 RESET 循环→代码10；修复=对齐参考工程补全 5 条(0x18/0x58/0x98/0xA0/0xE0, usbd_conf.c:332-340)。
- Q11 容量来自 CSD：sd_storage_get_capacity(usb_storage.c:90)→SD_GetBlockCount(sd_spi.c:415 CMD9 读 CSD)→READ CAPACITY 返回；开机 preinit 缓存([CAP] blocks=1941504)。
- Q12 "修 CBW 长度/签名"指设备端接收/校验侧 bug(PrepareReceive 31B+GetRxDataSize==31+签名 USBC, bot.c:225-228)，任何一环错→校验失败→Abort→stall→主机 RESET；非改主机格式。
- Q13 [INQ] op=12 重发非"没写响应"(36B inquiry+CSW 一直在)：是 Windows 认为事务未闭环从头重试——根因 CSW 后重布 OUT 收不到第二条命令(开发日志:287)；修复=IRQ(5,0)+Read10 重构+去 ISR 阻塞打印(deferred 让端点持续续跑)。
- Q14 只有 HID 无盘符：M3a 端点冲突(老 MSC 默认 IN 0x81 撞 HID→改 0x82/0x02, 日志:119) + PMA 缺 OUT(日志:101)。另有"磁盘可见无盘符+1-2分钟消失"=IRQ 优先级+Read10 双更新(日志:291-299)。
- Q15 "1-2分钟无法识别"非仅初始化顺序：五层叠加——SD 预初始化前置(解决 0xA/无盘符) + TUR 非零 dDataLength phase error 修复(日志:87-99) + IRQ (0,0)→(5,0)(日志:295) + Read10 双更新重构(日志:296) + 去 ISR 阻塞打印(日志:315-320) + deferred 任务化；M3 最终验证通过(日志:304-310)。

## 2026-09-06 第5讲 USB 答疑 2（用户 7 问：非阻塞完成确认/布OUT/IRQ优先级本质/PMA流程/TUR怪癖/Read10双更新）
- Q1 USBD_LL_Transmit/PrepareReceive 非阻塞：交给 PCD 硬件后立即返回；无轮询状态位，USB 栈纯中断驱动，完成=中断回调链 PCD_DataIn/OutStageCallback(usbd_conf.c:128-149)→Core→comp_data_*→MSC_BOT_*。任务侧用"信号接力"同步：Transmit 后回循环顶等信号，下一信号由下次 EP IN 完成中断经 SCSI_ProcessRead 送来。
- Q2 CBW 收不全当时两案：①PMA 缺 MSC OUT 端点缓冲(日志:101) ②CSW 后没重新布 OUT→第二条 CBW 收不到(日志:287)。
- Q3 布 OUT=USBD_LL_PrepareReceive 让端点进 armed 状态收下一包；BOT 每轮(收CBW→数据→CSW)后必须再布 OUT。核心原因非"高优先级阻塞"而是两点：A) ISR 做重活(早期内联 SD+阻塞打印,日志:124,315)拖延重布时机; B) IRQ 优先级违规——USB(0,0)最高抢占但 FreeRTOS 要求 FromISR API 的 ISR 优先级数值≥configMAX_SYSCALL_INTERRUPT_PRIORITY(5), 因临界区 BASEPRI=5 屏蔽不了 0 级→队列损坏/信号丢失→scsi_msc 永不唤醒→SD 不执行→该布的 OUT 没人布。危险非"必须长 ISR"，违反约束的短 ISR 也可能竞态，长 ISR 只是加重暴露。修复=(5,0)。
- Q4 PMA 分配流程(usbd_conf.c:298-343)：绑定句柄→dev_endpoints=8/SPEED_FULL→HAL_PCD_Init(MspInit 开时钟+NVIC 5,0)→注册回调→逐端点 HAL_PCDEx_PMAConfig 登记地址(0x00→0x18,0x80→0x58,0x81→0x98,0x02→0xA0,0x82→0xE0)。PMA=F103 USB 自带小块 SRAM, 所有端点缓冲从中划, 登记地址不重叠, 数据搬运在收发时进行; 改端点布局要重排地址。
- Q5 TUR 非零 dDataLength：操作码固定但 dDataLength 是 CBW 信封字段由主机填; Windows usbstor 对 TUR 发 0x60(预留96B 读回)系已知怪癖; ST SCSI_TestUnitReady 的 dDataLength!=0 防御检查→INVALID_CDB→Abort→CSW phase error 0x01→stall→ClearFeature 重试→1-2分钟; 修复=删检查, TUR 直接成功+bot_data_length=0, residue 自然=0x60 主机理解无数据。教训: 容忍主机不合规范但无害行为。
- Q6 Read10 双更新(日志:296)：bug=CBW 解析块写在 if(IDLE) 外 fall-through, 延续调用(DataIn 中断)时重复解析重置 scsi_blk_addr/len; 例: READ10 读 8 块→任务读完 LBA0 减到 addr=1/len=7, DataIn 中断又来→bug 版重置回 0/8→重复读 0 号块/残差错乱。Write10 一直 if/else 无此问题; 修复=Read10 同构。

## 2026-09-06 第5讲 USB 答疑 3（用户问：TUR 预留 96B 的原因/当时指令）
- 澄清：当时 SCSI 指令=标准 TEST UNIT READY, CDB(16B)=00 00 00 00...(仅 CB[0]=0x00), 指令体完全合规; 不合规的是 CBW 信封 dCBWDataTransferLength=0x60(96) + bmFlags=0x00。规范 TUR 无数据阶段 dDataLength 应为 0。
- 96B 原因(Windows 闭源, 基于事实+机制的解释)：①classpnp 用统一数据缓冲处理命令, 把缓冲长度直接搬进 CBW 不按命令裁剪(96B 接近其模式页/缓冲量级, 与 MODE SENSE 等共用模板); ②历史遗留一个缓冲多处复用(INQUIRY/MODE SENSE/TUR/READ FORMAT CAPACITIES 同一请求模板); ③微软不必修——BOT 的 dDataResidue 机制本就为这种不一致设计(设备应回 residue=96 表达"无数据"), 主机据此理解。正确设备响应=容忍+residue 表达, 而非拒绝+phase error。当初 bug=ST 库防御检查把主机越界声明当 INVALID_CDB→Abort→stall。

## 2026-09-06 全项目教学收尾（五讲速查总结，用户确认"可以总结了"）
- 定位：2.4G 空中鼠标+U盘一体(RX=STM32F103: nRF→HID 上报 + SD 双角色 U盘MSC/本地日志), USB 复合=HID Mouse+MSC 单口。
- 上电顺序铁律(main.c:98-124)：外设→NRF_Demo_Init→sysmon/IWDG→usb_storage_preinit(SD握手+容量缓存,必须在此)→scsi队列→MX_USB_DEVICE_Init(最后拉D+)→app_start。慢初始化全部放拉D+之前, 否则枚举时 SD 未就绪→411(0xA)/无盘符。
- 五讲速查：nRF(Init只配GPIO/Check 0xA5回读/TX·RX_Mode 寄存器/SPI全双工命令字节当次生效); SD(低速CMD0→CMD8→ACMD41 HCS→CMD58 CCS判SDHC→高速; CMD17/24+0xFE+忙等; CSD容量; 寄存器直写; s_hc 块/字节寻址; preinit提前+diskio+sd_log 3s错峰+单写者门控); FatFs(f_mount 立即挂载读MBR/DBR/BPB→f_open 8.3→f_lseek EOF=追加→f_write+f_sync; 簇=FAT项 表项=簇+2; MBR非必需; win[]=元数据write-back; MKFS=0 不自格式化); USB(USBD_LL_Init PMA+IRQ(5,0); 57B复合描述符 iface0 HID EP1IN中断/iface1 MSC EP2 bulk; comp_* pClassData切换; BOT CBW31→数据→CSW13; SCSI opcode分发; deferred ISR置标志+影子拷贝+唤醒 scsi_msc 任务续跑; FromISR 优先级须≥5)。
- Bug 总表：pack(1)对齐/HAL SPI 方向切换→寄存器全双工/SD 未就绪→preinit 前置/TUR 非零 dDataLength→删检查/缺 EP OUT PMA→补全/IRQ(0,0)违规+Read10双更新→(5,0)+if/else/ISR 阻塞打印→移除/MSC 端点撞 HID→0x82/M1-2分钟无法识别→供电(硬件)/SD_USE_MULTI 无提升→保持单块。
- 代码地图：驱动 NRF24L01.c/sd_spi.c; 应用 app_main.c/sd_log.c/usb_storage.c/usbd_composite.c/diskio.c; 启动 main.c; 裁剪 ffconf.h; USB底层 usbd_conf.c。

## 2026-09-06 第5讲 USB 答疑 4（用户问：USB与本地SD访问互斥实现/usb_busy/能否用互斥量）
- Q 现实现=usb_busy 时间门控(sd_log.c:33-36, USB_GATE_MS=1500ms; usb_storage_ping 在 get_capacity/read/write 刷新; sd_log.c:167 落盘前检查)。性质=软互斥/礼貌避让, 非严格互斥: USB 访问主机驱动突发且结束时刻不可预测, 用"最近活动时间窗"近似; 无锁/不阻塞 ISR/无优先级反转。
- 竞态窗口: sd_log 落盘(多块 CMD24, 每块 ms 级)中途主机发 READ10→scsi_msc 抢占→两任务同时在 SPI1 发命令→交错损坏。usb_busy 只避让"活跃后 1.5s", 挡不住"落盘中途 USB 突然活跃"。可跑靠场景容忍(日志尽力而为/USB 空闲不碰卡/落盘几十ms)。
- 用互斥量可以, 四纪律: ①只能在任务上下文拿锁(ISR 禁 lock; 本项目 MSC 已任务化故可行——deferred 的另一层收益) ②用带优先级继承的 mutex 非 binary semaphore(防优先级反转) ③持锁粒度=单块/单命令, 不锁整个文件传输(否则饿死 sd_log+scsi_msc 迟布 EP→主机 bulk 超时) ④覆盖所有 SD 访问者(含 SD_Init/容量查询), 漏一个白锁。
- 更干净方案: 单一 SD 服务任务+请求队列(所有消费者发请求排队串行执行)→消灭并发, 无锁/无反转/无竞态, 可顺带做调度(USB 优先)与块合并; 代价=架构改造大+队列要有界策略。
- 三档选择: 软门控(最小改动, 现状)/互斥量(严格正确, 改动可控)/SD 服务任务(架构彻底干净)。

## 2026-09-06 第5讲 USB 答疑 5（用户确认 mutex 用法：每块读写前拿锁/后放锁）
- 确认：方向正确——单块读写前拿锁、完成后放锁。三个精确化：
  ①别散落调用点手工 lock/unlock，封装进 sd_spi 对外 API(SD_ReadBlock/WriteBlock/Init/GetBlockCount)内部统一成对, 上层无感不漏。
  ②粒度取单块: 无竞争开销可忽略/持锁短(scsi_msc 最多等一块 ms 级, 不拖主机 bulk 超时)/满足核心保证(任何时刻单任务持有锁碰 SPI, 命令不交错)。锁整个文件传输会饿死 sd_log+拖死 USB 通路。
  ③连带深层问题: mutex 只解决"命令交错", 不解决 FatFs 缓存一致——sd_log 走 FatFs(写缓存, FAT/目录改在 win[] 里 f_sync 才落盘), MSC 是裸扇区直通; "FAT 已改未 f_sync"窗口内 USB 直读会读到旧扇区→Windows 看到不一致。现有 usb_busy 隐含"USB 活跃时 sd_log 不写"把窗口压小(log_flush 里 f_write 紧跟 f_sync 仅一瞬)。硬保证需: USB 访问前强制 f_sync 或 MSC 会话期间 sd_log 完全停写——锁替代不了这步。

## 2026-09-06 第4讲 sd_log 答疑（用户问：攒批为何不按"消息指向扇区"判断立即flush换扇区）
- 澄清关键误解：sd_log 数据模型=连续字节流(记录相邻拼接成 DATA.LOG 追加流), 不按"记录→扇区"映射; fbuf 只是字节暂存, 扇区/簇归属是 FatFs 职责。f_write 按文件逻辑指针连续写, 跨扇区/簇边界自动拆分/分配簇/多次 disk_write, 应用层看不到也干预不了。
- 若"两条消息落点扇区不同就立即 flush"→性能灾难: 每条 16B 立即写=写卡次数放大 32 倍(512/16)+每条触发 FatFs 查 FAT/簇+SD 编程 ms 级, 还会加剧与 USB 抢 SD。攒批=内存换写卡次数。
- 512B 上限=恰好一扇区, 攒满大概率整块落盘, 减少半扇区读-改-写碎写——这是对齐的附带收益, 非"每条对应扇区"语义。
- 跨扇区真实机制: f_write 内部按扇区切分, 完整扇区直接 disk_write, 首/尾不满扇区做读-改-写(读扇区进窗口改部分再整扇区回写, 保护同扇区其他数据)。
- 落盘节拍与扇区无关: 缓冲满512B 或 距上次flush≥1s(最多丢1s) 且 !usb_busy; flush=f_write+f_sync(后者刷 FAT/目录脏窗口)。

## 2026-09-06 存储模型探讨答疑（用户问：真有"每条数据各写不同扇区"的极端需求是否很难处理）
- 关键洞察: "每条独立扇区"与"通用文件系统高效攒批"本质是两种存储模型, 诉求矛盾时先选型而非硬凑。
- 模型对比: 顺序追加流(连续拼流, FS 管扇区, 日志/流水, 本项目 DATA.LOG) vs 定长槽位/直接寻址(每条固定块, 按编号随机读写可覆盖, 黑匣子/遥测环形缓冲/键值/参数区)。每条 16B 硬塞 512B 扇区=利用率 3%+写放大 32 倍, 确属浪费。
- 真需求的标准做法(底层 SD_ReadBlock/WriteBlock 本就按扇区寻址, 单条独立扇区不难, 难在又要FS又要独立扇区): ①不要 FS→绕过 FatFs 裸块管理+自管"编号→扇区"索引; ②要 FS 又要固定位置→大文件 f_lseek(固定偏移)+f_write(FAT 支持任意偏移写, 落点仍 FS 编排); ③环形覆盖→固定块数裸块环形缓冲+头指针。
- 本项目无此需求原因: sd_log 纯顺序追加+整文件顺序解析, 无按编号跳读诉求→攒批最优; 其余任务不写 SD 业务数据; 真有"单条防丢"诉求正解是提高 flush 频率非独立扇区。

## 2026-09-06 存储模型答疑 2（用户复述确认："一个数据一个扇区"实现难度）
- 对一半需纠正：SD_WriteBlock(block,buf) 参数即 LBA 扇区号→裸块直接任意扇区, 物理保证, 对。但 FatFs 层不存在"当前扇区指针", 应用只能控制文件写指针 fptr(字节偏移, f_lseek 移动); 物理扇区由 FatFs 按簇链映射内部决定, 应用指定不了物理扇区号。
- FatFs 定长槽位正确姿势: ①首次预扩展文件到全长(直接 lseek 超 EOF 写, FAT 空洞内容未定义非干净 0, 须先扩满) ②f_lseek(&file, i*SLOT_SIZE)+f_write+f_sync。逻辑固定位置 OK; 物理独立扇区仅当 SLOT_SIZE=512 且簇=1 扇区时恰好成立, 非 API 保证。
- 结论对照: 逻辑独立位置=FatFs lseek+f_write(不保证物理); 物理独立扇区=绕过 FS 裸 SD_WriteBlock(保证)。两路冲突点: 绕过 FS 则 Windows 无盘符, 故"物理独立扇区+可当U盘"工程上很少同时要。

## 2026-09-06 答疑：link_loss_ratio 在哪里实现/语义
- 实现：sysmon.c 统计打印 + app_main.c rf_rx 喂数。计数源 app_main.c:42-57(rf_rx 10ms 轮询 NRF24L01_RxPacket==0→sysmon_rx_ok / 空→sysmon_rx_idle); sysmon.c:22-27 volatile 计数+自增; :85-88 每秒 [MON] 打印 link_loss_ratio = rx_idle*100/(rx_ok+rx_idle)。
- 语义澄清：这不是真丢包率, 是"轮询空转率/链路空闲度"近似。rf_rx 轮询非 IRQ 接收; TX 事件驱动发包(鼠标不动不发)时 RX 大部分轮询落空→94% 属正常(开发日志:273 明示)。它无法区分"TX 没发"与"发了丢了"。
- 真丢包率需 TX 包序号 seq 配合: RX 端统计 seq 跳变(期望seq-收到seq 累加)。日志 LogRecord 已含 seq 字段但未做跳变统计。
- sysmon 还管 IWDG: vApplicationIdleHook 空闲钩子喂狗(sysmon.c:68-71) + mon_task 每秒喂(:105) 双保险; 每10s CPU 运行时间统计, 每20s 任务栈水位 vTaskList。

## 2026-09-06 Bug 分析：USB 接入时 DATA.LOG 不更新/删除后不新建
- 代码确认: sd_log 挂载+ f_open(FA_OPEN_ALWAYS) 一次(sd_log.c:136)后永久持有 FIL 句柄, 无 f_stat 检测外部删除/改名, 无 USB 连接回调挂钩卸载; usb_busy 门控仅暂停落盘不处理句柄失效。
- 症状①不更新: USB 访问刷新 last_active→usb_busy 持续为真→sd_log 被门控暂停; 加 Windows 目录显示缓存。症状②删了不新建: Windows 删文件=改目录项 0xE5+FAT 簇空闲, 设备侧句柄无感知→永不重新 f_open; 且 sd_log 继续写旧簇(Windows 视为空闲可能重分配)→互相覆盖损坏; f_sync 可能写回旧目录项→"删了复活"。
- 根因: 单卡双 FS 客户端(设备 FatFs + Windows MSC 扇区直通)并发, 各自缓存+打开状态, 无会话切换/句柄失效机制; usb_busy 只解决命令交错不解决缓存一致(呼应"锁管不了缓存一致性")。
- 方案: A(推荐) USB 会话互斥——检测 USB 连接→flush+f_close+f_mount(NULL) 彻底卸载进入挂起(卡给 Windows 独占), 断开→重挂载继续; attach/detach 信号挂 PCD_Connect/DisconnectCallback 或首次 SCSI 访问。B 模式二选一(日志模式不挂 MSC/反之) 更彻底。C f_stat 周期检测=补丁仍有竞态不推荐。核心: 两 FS 客户端同时可写无安全模型, 必须独占切换。

## 2026-09-06 架构讨论：U盘与日志并存 vs 鼠标隔离（用户问：USB枚举一起怎么隔离）
- 纠正关键误解: ①HID 不走 SD 卡(EP1 IN 直发), 鼠标与日志永不冲突; ②MSC"枚举/盘符存在"≠占用卡, Windows 空闲基本不碰卡, 占用只发生在真实 READ10/WRITE10(打开盘符/拷贝/删除/扫描)。真正冲突=Windows 读写卡的时段 vs sd_log 写卡时段。
- 隔离正解: 隔离"MSC 介质可见性"而非 USB 物理链路。类比 USB 读卡器: 控制器常驻+介质可选——空读卡器枚举成功但无盘符(SCSI 报无介质)。实现: 日志模式(默认)→usb_storage 的 is_ready/capacity/TestUnitReady 报介质未就绪→Windows 不分配盘符不碰卡→sd_log 独占; U盘模式→sd_log flush+卸载→报就绪→Windows 轮询 TUR 检测介质插入自动挂载(可移动介质热插拔是标准语义, 无需重插 USB)。HID 全程不受影响。
- 切换触发: 物理按键/串口命令/规则自动(真实 SCSI 读写即切, 但有打开瞬间竞态)。
- 若要求"插着随时看盘符+日志不停"=无解(Windows 可随时写卡), 唯一彻底解=双存储(U盘用SD, 日志写片内Flash/第二张卡/SPI Flash)物理隔离。
